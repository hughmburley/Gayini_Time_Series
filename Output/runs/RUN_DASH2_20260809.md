# RUN — DASH2 + the consolidation pass, 9 August 2026

**23 sheets rendered and registered.** `D1v2_paddock_*_slide_data`.
**Ruling BL honoured** — the 21 `D1_paddock_*` sheets and their 21 registry rows are
untouched; v1 renders byte-for-byte unchanged because every new argument defaults off.

**Rulings applied:** AZ, BB, BL, BX, CX, CZ, DA, DB, DJ, DQ, DS, DT, DU, DV, DW, DX, DY,
DZ, EA. **Outstanding: none.** One ruling is **not met** — DY, see below.

---

## Gates

### DZ check 1 — TEMPORAL-1 x against the dashboard top panel · **GATE OPEN**

**Max |Δ| 0.0173 pp across all 23 units; 23/23 pass.** Same population, same definition,
and the two products agree. Neither is wrong.

### DZ check 4 — the printed subset percentage · **stopped the gate, and is fixed**

**Your prediction is confirmed and the denominator is identified.** The panel divided by
**all classes**; the sheet's own scope is **non-treed**. The three flagged units:

| | sheet said | ÷ all classes | ÷ non-treed (correct) |
|---|---:|---:|---:|
| Dinan 9 | 56% | 56.0% | **60.7%** |
| Dinan 10 | 54% | 54.3% | **65.1%** |
| Bala 29ca | 33% | 32.7% | **34.6%** |

Bala 26ca, Mara 21 and Bala 8/11 agreed **because they have no treed cells** — the two
denominators are identical there. That is the whole pattern.

This changes a printed percentage, so it stopped the gate. **Fixed**: the sheets now
print the non-treed share, read from `DASH2_DZ_reconciliation.csv` so a sheet and the run
record cannot drift apart. `gayini_veg_water_census_panels.R` was **not** edited — the
correction is a post-hoc label override in the v2 branch.

### DZ check 2 — subset against whole unit · **the stated diagnosis is wrong**

16 of 23 exceed 0.5 pp. **DZ attributes this to per-cell valid-year denominators varying.
It measures otherwise: `valid_years` = 35 for every in-zone non-treed cell, so
denominators do not vary at all.**

The deltas track subset share almost perfectly — Bala 26ca (98.1% subset) +0.63 pp,
Bala 29ca (34.6%) +7.39, Mara 8 (50.1%) −11.78. **These are population differences, which
is exactly what DU says**, not a denominator artefact. Recorded and continued, as DZ
directs, but under the corrected cause.

**DU's identity verified directly:** over the *same* population, the mean of annual shares
equals the mean over cells of between-year frequency — **max |Δ| 0.0139 pp on all 23**.

### DZ check 3 — TEMPORAL-1 y against the dashboard floor

Applicable on **5 of 23** units (subset ≥ 99%); **5/5 pass**. Below 99% the difference is
expected under DB and is recorded in the table, not treated as failure.

### DW — the boxplot marker · **removed, per the pre-registered fallback**

**The marker did not reconcile.** It is `gayini_unit_flood_frequency()` — the **whole
polygon on the native 28355 stack** — while the top panel and gauge are the paddock's
census cells on 8058. Dinan 10: marker **10.1%** against a top panel of **5.1%**.
Bala 29ca: **10.3%** against **8.5%**.

Redefining it to the cell-based value would reconcile it arithmetically, **but the
boxplot cloud is plot support (66 monitoring plots)** — that would put a pixel number on
a plot cloud, reintroducing on this sheet the exact support mix DASH2 exists to remove.
**Marker removed; the boxplots stand as community context**, with the reason on the panel.

A consequence worth noting: with the marker gone, **the gauge and the top panel now agree
on every sheet** — Dinan 10 reads long-run 5% against a 35-year mean of 5%.

---

## The DU acceptance case — Bala 29ca

On the sheet, in this order: the top panel's **35-yr mean 9%**; the response panel's
subtitle *"It is wet 16% of years, while the paddock as a whole is wet 9% — both are
right; they describe different ground"*; and the panel's own corner reading **flood freq
15.9% · n = 12,687 census px**. **A reader can see both numbers and why they differ.**

---

## Overrides

**§3.2 not applied** (accepted in adjudication) — the "Where it sits" panel plots a
genuine plot-support between-year frequency and was already correctly labelled.

**DZ check 2's diagnosis overridden** — denominators do not vary; the cause is population,
per DU. Evidence above.

**DY's fix attempted on the wrong path** — see Not done.

## Applied this pass

**DT** — the correspondence sentence (*"a correction … is going to the client
separately"*) is off every sheet face; it lives here instead.
**DU** — face wording as above, subset share adjacent to the numbers.
**DV** — one common y-scale, 0–100, on the response panel across all 23.
**DX** — the tercile sentence moved from the header to the footnote block, in plain
words: *"bands on the map are cut separately for each plant community, and a small share
of cells falls either side of a band edge if the alternative water surface is used."*
**"interpolated surface" and "balanced strata" do not appear on any face.**
**EA** — no issue codes, ruling letters, `number_id`, `fit_id` or repository paths on any
face. **The `(C10)` on the TEMPORAL-1 support footnote is not yet removed — see Not done.**

## Not done

**DY is NOT MET.** The locator inset **clips the top latitude label** on maps whose extent
reaches the top-left — Bala 26ca is the clearest case. Verified by opening four sheets
(Bala 29ca clean, Dinan 10 clean, **Bala 26ca clipped**, Dinan 9 clipped), not by exit
code.

**The cause is identified and my fix went to the wrong place.** I added an additive
`inset_x` to `gayini_panel_map()` and threaded it from the composer; it had no effect
because **the paddock checkerboard draws its own locator through a second path,
`R/gayini_area_map.R:215–217`, with its own `ins_local_x0`.** Two attempts at widening the
offset moved nothing, which is what sent me to look for a second call site. The correct
fix is an additive parameter on that function, threaded through — **not attempted here**,
because it needs another full re-render and I stopped rather than make a fourth pass at a
placement issue.

**EA on TEMPORAL-1** — the `(C10)` removal from that figure's footnote is outstanding.

**Caption register** — not updated for the changed captions.

## Artefacts

| | |
|---|---|
| 23 × `D1v2_paddock_*_slide_data.png` / `.pdf` | registered, `run_id = DASH2_20260809` |
| `Output/runs/DASH2_before_after.csv` | before/after water means, coverage |
| `Output/runs/DASH2_DZ_reconciliation.csv` | all four DZ checks, 23 units |
| `R/gayini_dash2_panels.R` | new, additive |
| `R/gayini_dashboard_compose.R`, `R/gayini_dashboard_panels.R` | additive arguments, defaults preserve v1 |

**`figure_asset`: 23 DASH2 rows, 0 NULL `support_level` / `figure_level`**, pixel
throughout. 21 v1 rows unchanged.

**Ruling DS** — every edit in this pass was written to a file and applied from it, never a
shell heredoc, and parse-checked before rendering. It caught one of its own: a generator
wrote the token `BS_N` into the R source instead of the escape, which parsed fine and
failed at runtime.
