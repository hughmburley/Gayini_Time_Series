# Task K — Gate 0: independent Output census — report

*Execution seat, workstation, 19 July 2026. Branch `tier2k-gate0-output-census`. **Read-only gate — held for human review, no merge.***

---

## 0. Read-only proof (acceptance #3)

| | SHA-256 of `Output/database/Gayini_Results.sqlite` |
|---|---|
| **Start of run** | `37c5d72de77ac9b10e7d38a787d32e725268624ed9d60f7890b5c0b4cca695e7` |
| **End of run** | `37c5d72de77ac9b10e7d38a787d32e725268624ed9d60f7890b5c0b4cca695e7` |

**Byte-identical.** The DB was opened `mode=ro` throughout; the builder was not run; no row was written to any table. The only writes are the six `taskK_gate0_*` deliverables (five in `Output/diagnostics/`, this report in `docs/change_reports/`).

*Method note:* this is a **full-file** hash for the integrity proof, distinct from the builder's 50 MB-capped content hash discussed in §4.

---

## 1. Verdict (one line)

**The design seat's structural picture is correct and holds. Every DISK count in §4.1 DIFFERs — but by a single, coherent cause: a two-day snapshot drift (17 → 19 Jul) dominated by Task J outputs. No registry, builder, code, or Gate A claim is refuted. The two most consequential claims — the MER/MODIS-CSV essentiality trap and the "431 files / 0 registry rows" Gate A baseline — are both CONFIRMED.**

---

## 2. DIFFER table (this section first)

Every claim that did not cleanly match, measured vs claimed. **None of these is a defect in the design seat's method** — the disk simply moved on two days past its 17 Jul snapshot.

| # | Claim | Measured (19 Jul, live disk) | Verdict | Why |
|---|---|---|---|---|
| D1 | 1,326 files · 158 folders · 1.569 GB | **1,351 files · 172 dirs (146 with files) · 1.818 GB (1.693 GiB)** | DIFFER | 2-day drift: Task J, `sampling_rebalance_smoke`, loose Output-root files |
| D2 | figures 300 / 21 folders / 131 root / 169 sub | 304 / 24 / **131 root (match)** / 173 sub | PARTIAL | root count exact; totals drift up via `figures/*/task_J` |
| D3 | ladder anywhere 330 / root 122 | 340 / 126 | DIFFER | drift |
| D4 | 101 stems · 161 pdf/155 png/**14 svg** | 103 stems · 166 pdf/160 png/**14 svg** | DIFFER | svg count exact; pdf/png drift |
| D7 | fc_intermediate 6 files · 0.759 GB · 48.4% · **0 registered** | 6 files · 0.815 GB · 44.8% · **0 registered** | PARTIAL | file count + 0-registered exact; bytes larger (regenerated), % lower (bigger denominator) |
| D8 | dup filenames 382 files · 102.3 MB | 388 · 107.4 MB | DIFFER | drift |
| D9 | 13 one-file · 16 zero-file · 47 (30%) ≤2 | **13 one-file (match)** · 26 zero · 60 (34.9%) ≤2 | PARTIAL | one-file exact; drift added empty wrapper dirs |
| D10 | every figures **subfolder** file ≤ 9 Jul | figures subfolders now hold **18-Jul** files | DIFFER (refuted) | **Task J** wrote `figures/maps/task_J` + `figures/plots/task_J` on 18 Jul |
| R2 | orphan 1,039 (78%) | 1,064 (**78.8%**) | MATCH on %, DIFFER on count | proportion stable; absolute tracks file count |
| R8 | `map_asset_index` stale — compare its counts | **table absent from live DB** | DIFFER (void premise) | builder *creates* it (`:2750`) but the live DB has no such table — comparison impossible |

Everything else in §4 is a clean **MATCH** or **CONFIRMED** (tables below).

**The drift, isolated.** The four Task J figures dated 18 Jul (`J-F1_2018_difference_map.png`, `J-F2_placebo_ladder_six_panel.png`, `J-F3_the_law.png`, `J-F4_annual_series.png`) plus `rasters/task_J/` (12 files), `diagnostics/sampling_rebalance_smoke/` (10), and five loose Output-root files (`Output_search.txt`, `messages.txt`, `cleanup_R_terra_temp.ps1`, `figures.zip`, `Gayini_analysis_variable_LUT_20260715.xlsx`) account for the +25 files. This is exactly what an independent census on the live disk is supposed to surface: the design-seat numbers are right *as of 17 Jul*, and are now two days stale.

---

## 3. Broken pointers — the non-negotiable baseline

**0 broken pointers.** All 287 registered paths (139 figure + 109 raster + 38 report + 1 census) exist on disk. Re-derived independently against my own disk walk (not trusting the stored `path_exists` flag). **Matches R3.** Full evidence in `taskK_gate0_registry_join_20260719.csv`.

---

## 4. Checksum verify — the check that had never run

**283 of 285 pass. 2 fail.** Full evidence in `taskK_gate0_checksum_verify_20260719.csv`.

| | |
|---|---|
| Method | Full-file SHA-256 recompute vs recorded `checksum_sha256`, across all four asset tables |
| Recorded non-null checksums | 285 (figure 139, raster 107 of 109, report 38, census 1) |
| Pass | 283 |
| Fail | 2 |

**The 2 failures — a DB finding, not corruption:**

1. `Output/reports/spine_smoke_test/spine_smoke_test_report.md` (454 B)
2. `Output/reports/database/Gayini_results_database_build_report.md` (7,134 B)

Both are **auto-regenerated report files** whose on-disk content drifted after the checksum was recorded at build time. Not damage — stale registry rows. A later gate decides whether these are re-registered or excluded from the manifest.

**The §5 truncation trap does NOT arise — correction to the spec's framing.** The builder's `sha256(path, max_bytes=50MB)` (`:195-209`) **returns `NULL` once a file exceeds 50 MB — it does not store a truncated hash.** Consequently **no non-null recorded checksum belongs to a >50 MB file** (the two null-checksum rasters are the >50 MB / NULL-run_id ones). Full-file comparison is therefore correct for all 285, and the feared "every raster over 50 MB mismatches spuriously" cannot happen against this DB. The 381 MB / 353 MB `fc_intermediate` files carry no registry row and no checksum at all.

---

## 5. `workstream` × `essential` — and the trap

**🔴 The central trap is CONFIRMED, loudly.** `workstream = MER` (or `pre_post`, or `old_deck`) does **not** imply `essential = no`. **14 files are `essential = yes` while sitting in a dead workstream** — every one a migration blocker. Listed by name:

| # | File | Workstream | Why essential |
|---|---|---|---|
| 1 | `Output/csv/MER/mer_annual_max_by_plot.csv` | MER | builder_csv_input |
| 2 | `Output/csv/MER/mer_period_summary_by_plot.csv` | MER | builder_csv_input + path_module |
| 3 | `Output/csv/MER/mer_period_summary_by_vegetation_group.csv` | MER | builder_csv_input |
| 4 | `Output/csv/MER/mer_vs_annual_occurrence_raster_comparison_by_plot.csv` | MER | builder_csv_input + path_module |
| 5 | `Output/csv/review_deck/mer_metric_comparison_table.csv` | MER | builder_csv_input |
| 6 | `Output/csv/ground_cover/10a_ground_cover_prepost_plot_summary.csv` | pre_post | builder_csv_input + path_module |
| 7 | `Output/csv/ground_cover/10a_ground_cover_prepost_plot_summary_interpretation.csv` | pre_post | path_module_resolved |
| 8 | `Output/csv/review_deck/candidate_dashboard_set_for_review_updated.csv` | old_deck | path_module_resolved |
| 9–11 | `Output/rasters/MER/period_summaries/mer_{pre,post}_annual_max_observed_frequency_pct.tif`, `mer_post_minus_pre_annual_max_frequency_pct_points.tif` | MER | path_module_resolved |
| 12–14 | `Output/rasters/inundation_pre_post/{pre_,post_,post_minus_pre_}*.tif` | pre_post | path_module_resolved |

Plus the **2 MODIS CSVs** (`03_modis_ground_cover_context_full.csv`, `modis_context_units_summary.csv`) — `workstream = MODIS`, `essential = yes` (builder_csv_input). **The spec's headline claim is TRUE: four MER CSVs, one review-deck MER CSV, and two MODIS CSVs are load-bearing builder inputs; MER figures and rasters are the superseded generation, but these CSVs are not expendable.**

**Nuance on the pre/post and MER *rasters* (rows 9–14):** they are essential *only because `gayini_output_paths.R`'s `task15_input_specs()` still resolves them as first-existing candidates* — i.e. the retired-pre/post path resolver still points at them. That is itself the finding: retired products remain wired into a live resolver. Whether that resolver is still called by any current-ladder script is a code-tracing question a later gate should close; per rule 4 I marked them `yes` as the spec directs, not `unknown`.

**Crosstab (essential yes / no per workstream):**

| workstream | yes | no | total |
|---|---:|---:|---:|
| tier1_bundles | 0 | 305 | 305 |
| MER | 8 | 168 | 176 |
| background_scenarios | 0 | 176 | 176 |
| ALL_PIXEL | 65 | 44 | 109 |
| MODIS | 2 | 82 | 84 |
| LADDER | 77 | 0 | 77 |
| old_deck | 1 | 73 | 74 |
| repo_audits | 0 | 73 | 73 |
| pre_post | 5 | 63 | 68 |
| RS_coverage | 0 | 44 | 44 |
| hydrology | 2 | 37 | 39 |
| infrastructure | 2 | 26 | 28 |
| ground_cover | 2 | 13 | 15 |
| unclassified | 4 | 79 | 83 |
| **total** | **168** | **1,183** | **1,351** |

The 4 `essential = yes` in `unclassified` are all builder inputs my rules didn't bucket: `csv/canonical/curated_annual_inundation_timeseries.csv`, `csv/canonical/curated_daily_inundation_monthly.csv`, `csv/canonical/plot_rs_analysis_base.csv`, `csv/inundation/inundation_frequency_by_vegetation_group.csv`. All 16 CSV_INPUTS are present → the builder will not throw on missing input.

---

## 6. The `unclassified` residue

**83 files unclassified (my rules classify 93.9%).** This is a **DIFFER from the design seat's 79% (272 unclassified)** — and per the spec, the delta is itself a finding.

**Why my number is ~15 pp higher.** Two rule choices, both stated so they can be overridden:
1. **All `review_bundles/` → `tier1_bundles`** (+305 files). The design seat appears to have left much of this generic.
2. **`diagnostics/` subfolders classified by embedded workstream tokens** (`MER`, `MODIS`, `background`, `RS_coverage`) rather than left generic. The design seat's 272 were concentrated in exactly `diagnostics/`, `logs/`, `csv/inundation/`.

Neither rule *forces* a file into a bucket against evidence — they follow a genuine path signal. But if the design seat prefers the conservative reading (workstream by top-level folder only), its 79% is equally defensible. **This is a method difference, not a data disagreement.** My residue concentrates as claimed — top folders: `Output/diagnostics` (13), `diagnostics/sampling_rebalance_smoke` (10), `diagnostics/check_curated_outputs` (9), `reports/figure_refresh` (9), `rasters/fc_intermediate` (5). A rule that would genuinely help: classify `sampling_rebalance_smoke` → a new `sampling_rebalance` workstream (it is Task-F-adjacent rebalance evidence), and leave true one-off diagnostics unclassified.

---

## 7. Folder shape

| Metric | Value |
|---|---|
| Total directories (incl. Output root + empties) | 172 |
| Directories holding ≥1 file | 146 |
| **Zero-file (pure wrapper) dirs** | **26** |
| One-file dirs | 13 |
| ≤2-file dirs | 60 (34.9%) |
| Deepest chains | `Output/_archive/reports/ppt_asset_packs/ppt_asset_pack_20260625/0*_…` (depth 5) |
| Worst files-per-folder | `rasters/MER/annual_max` (57), `rasters/inundation_background/*/annual` (54/52), `diagnostics` (48), `figures/maps/modis_ground_cover` (46) |

Full per-folder table in `taskK_gate0_folder_shape_20260719.csv`. The 26 zero-file wrappers (up from a claimed 16) are drift plus the `_archive/reports/ppt_asset_packs/*` skeleton.

---

## 8. Builder hazards — B1–B6, each with the line I read

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| B1 | `main()` → `reset_file(SQLITE_PATH)` → `path.unlink()`; DB deleted+rebuilt every run | **CONFIRMED** | `reset_file` = `if path.exists(): path.unlink()` at `:250-252`; called `reset_file(SQLITE_PATH)` at `:3157` |
| B2 | asset discovery = unfiltered `rglob("*")`, suffix filter only; the 139 is a stale snapshot | **CONFIRMED** | `:1463` base dirs `[csv, figures, reports, rasters, diagnostics]`; `:1466` `for path in base.rglob("*")`; `:1467` suffix-only filter |
| B3 | `map_asset_index` has a SECOND independent `rglob` — exclusion is two edits | **CONFIRMED** | `:2750` `CREATE TABLE map_asset_index`; `:2755` second `rglob("*")` |
| B4 | routing by suffix: PDF → `report_asset`; so ladder PDFs register as *reports* | **CONFIRMED** | `:1518-1519` `.png/.jpg/.jpeg → figure_asset`; `:1520-1521` `.md/.pdf/.pptx/.xlsx → report_asset`. **166 ladder PDFs on disk would land in `report_asset`.** |
| B5 | `census_asset` appears 0 times in the builder; a rebuild drops it | **CONFIRMED** | grep `census_asset` over the builder = 0 matches |
| B6 | a rebuild cannot reproduce 12 post-build rows | **CONFIRMED** | live DB carries 9 `crs_epsg=8058` rasters + 2 NULL-run_id (28355 stack) + 1 `census_asset` = 12 rows the builder's METRICS/discovery cannot regenerate; `crs_epsg/product/legend_status` lost with them |

**B1 + B5 are exactly why the builder was not run in this gate.** B1 is *true* (reset, not upsert) — no major-finding stop was triggered.

---

## 9. Gate A readiness — the claim the whole gate rests on

**CONFIRMED, exactly.** The design seat's proposed archive set is **431 files / 43 folders / 0 registry rows / 0 essential**:

| Group | Files | Folders | Registered | Essential |
|---|---:|---:|---:|---:|
| `review_bundles/tier1*` | 274 | 32 | **0** | 0 |
| `rasters/inundation_background` | 138 | 6 | **0** | 0 |
| `figures/review_refresh` | 19 | 5 | **0** | 0 |
| **Total** | **431** | **43** | **0** | **0** |

**Not one of the 431 carries a registry row or an `essential = yes` flag.** The claim the migration rests on holds. One correction to the arithmetic: because the live disk is at 1,351 files (not 1,326), the after-archive total is **1,351 → 920** (not 1,326 → 895) and dirs **172 → ~129**. The archive-set counts themselves are exact; only the before/after totals shift with the drift.

---

## 10. Registry & code claims (R and C) — full verdicts

**Registry:** R1 MATCH (287) · R2 MATCH-on-% (78.8) · R3 MATCH (0 broken) · R4 MATCH (1 run_id `db_build_20260701_114458`, 0 ladder-prefixed in `figure_asset`, all ladder files unregistered) · R5 MATCH (98/6/2/1/2 provenance split) · R6 MATCH (`veg_regime_class_8058.tif` registered → facts §11 **C2 trap is stale**) · R7 MATCH (5 absolute `D:\` paths; `spatial_005` → `Murrumbidgee_Gauge_Workflow`, a different repo) · **R8 DIFFER — `map_asset_index` absent from the live DB.**

**Code:** C1 CONFIRMED w/ correction (9 functions default `out_dir` to the figures dir — **7 in `gayini_descriptive_figures.R`, 2 in `gayini_sampling_design_map.R` via `gayini_path()`, 0 in `vector_prep_functions.R`**; the count is right, the third file in the claim has none) · C2 CONFIRMED (fallback resolver, `found_preferred/found_fallback`, emits a `checks` tibble with `status`) · C3 CONFIRMED (`scripts/_deprecated/01_lag_diagnostics_inundation_gc.R` present; the spine smoke test asserts `scripts/archive/` *absence*, so it does **not** catch `_deprecated/`) · C4 CONFIRMED (`GAYINI_ROOT` defaults to `D:/Github_repos/Gayini` in 4 files under `scripts/09_qa/`) · C5 CONFIRMED (both 09_qa checks touch retired pre/post: `01` reads `rasters/inundation_pre_post`; `02` impl references `pre_/post_conservation` frequency at `:409-410`) · C6 CONFIRMED (two shadow registries — `gayini_gradient_helpers.R:167` writes `Output/figures/figures_manifest.csv`, present on disk; `gayini_figure_manifest.R` has the richer `figure_id/deck_section/intended_slide/caption/caveat/qa_status` schema) · C7 CONFIRMED (`06_MER_inundation`, `12_lag_diagnostics` written by code, absent on disk) · C8 CONFIRMED (`csv/canonical/plot_rs_analysis_base.csv` present, mtime 24 Jun, `stg_canonical_plot_rs_analysis_base` = 66 rows; the "missing from the main RS repo" note refers to a *different repo's* location, not this Output copy).

---

## 11. What I could not determine (`unknown` beats a guess)

1. **Whether `gayini_output_paths.R`'s pre/post + MER raster candidates are actually reached by any current-ladder script.** I marked them `essential = yes` per rule 4 (resolver resolves them) but did not statically trace call sites into live ladder code. If no current script calls the resolver, blockers 9–14 downgrade from "load-bearing" to "referenced by dead code" — a code-tracing job for a later gate.
2. **Why `map_asset_index` is absent** though the builder creates it (`:2750`). Either a post-build step drops it, or this DB predates that builder code. Not resolvable read-only without build history.
3. **The design seat's exact workstream rules.** My 93.9% vs 79% is reconciled by *plausible* rule differences (§6), not the actual ruleset — I could not see it.
4. **`Output/Gayini_analysis_variable_LUT_20260715.xlsx`** (loose at Output root): a reference LUT with no static code reference in `Output/`. Marked `essential = no`, but a doc/deck may cite it — closer to `unknown`. Flagged for the migration owner.

---

## 12. Deliverables (all committed on `tier2k-gate0-output-census`)

| # | Path |
|---|---|
| 1 | `Output/diagnostics/taskK_gate0_census_20260719.csv` (1,351 rows) |
| 2 | `Output/diagnostics/taskK_gate0_registry_join_20260719.csv` (287 rows) |
| 3 | `Output/diagnostics/taskK_gate0_checksum_verify_20260719.csv` (285 rows) |
| 4 | `Output/diagnostics/taskK_gate0_folder_shape_20260719.csv` (172 rows) |
| 5 | `Output/diagnostics/taskK_gate0_qa.json` |
| 6 | `docs/change_reports/taskK_gate0_20260719.md` (this report) |

**Nothing was moved, renamed, deleted, or registered. The builder was not run. Held at the gate for human review — no merge.**

*Spec-doc note: `docs/Gayini_output_structure.md` (named in the spec's "read first") does not exist; the nearest artefact is `Output/reports/code_and_repo_audits/output_audit/proposed_output_structure.md`. Flagged, not acted on.*
