# Gayini — issues log

**Started:** 27 July 2026 · **Deadline:** 10 August 2026
**Purpose:** capture build and process issues without letting them stop delivery.
**Scope:** build, process, tooling and paper-trail defects. **Scientific limitations belong in `Gayini_limitations_register_*.xlsx`** (currently v10, 43 rows). A row here graduates to that register if it turns out to constrain what the science can claim.

---

## The triage rule

At any gate, one question decides whether an issue stops work:

> **Does this change a number that reaches a deliverable?**

- **Yes → resolve before the gate closes.** A wrong number is more expensive later than the delay is now.
- **No → log it, state the disposition, proceed.**

That is the whole rule. It is deliberately mechanical so it can be applied in seconds without debate.

Two corollaries, both learned the hard way:

- **"Would be better" is not "is wrong."** Improvements raised at a STOP point read as urgent because of where they appear. They are not. Log them as `IMPROVE` and move on.
- **A number reaching a deliverable includes the paper trail for that number.** A figure nobody can reproduce is a number that doesn't reach anything.

### Severity

| | Meaning | Action |
|---|---|---|
| **BLOCK** | Wrong output if ignored | Resolve now |
| **DEFECT** | Real, but current output is correct | Log, fix when cheap |
| **IMPROVE** | Would be better | Log, revisit post-deadline |
| **EXTERNAL** | Waiting on someone else | Log, chase, work around |

### Review cadence

At each task boundary (not each gate): scan `BLOCK` and `EXTERNAL` only. Full review once weekly and once post-deadline.

---

## Open

| ID | Area | Sev | Issue | Affects a deliverable number? | Disposition |
|---|---|---|---|---|---|
| **I-38** | Provenance / aggregation | **DEFECT — mechanism, not instance** | **`aggregation_order` is the mechanism behind the historical number discrepancies, and it is invisible in a value.** Worked case, R2 prep: the register's post/pre inundation pair **43.6% / 22.8%** did **not** reproduce as mean-of-paddock-means, which gives **46.0% / 25.2%** — a 2.4 pp gap on both terms with *identical* inputs, filters, period and support. It reproduces **exactly** as pixel-weighted `SUM(wet_pixels)/SUM(valid_pixels)`. The n's (4 post, 31 pre) matched under both, so the usual tell — a wrong denominator — was absent. Two defensible aggregations of one quantity differ by more than most of the twelve discrepancies the T1 audit catalogued. | **Yes** — 43.6/22.8 is in the pack's Q7 answer cell, and the same ambiguity sits under every paddock-to-property rollup | **`dim_headline_number.aggregation_order` already exists as a column and is the fix — it is under-used, not missing.** Standing rule: **any number that rolls paddocks up to the property states its aggregation order in the same breath as its value**, because the value alone cannot distinguish them. Every pin registered at R2 carries the column populated. Neighbour of C-07 (composite support_level) and the two-floors pair — one name, several quantities. |
| **I-39** | Registries / rules | IMPROVE — **I-17 family** | **A selection rule that picks numbers to pin had no "and does not already have a `number_id`" clause.** R2's pin rule (*pin a SOURCED number if quoted in more than one deliverable, or if it is a numbered register §1 claim*) selected 10 candidates; **3 were already pinned** — `t10_gap_annual_slope_C_29ca`, `t13_parts_declining_count`, `t10_bala29ca_xsec_residual`. Registering them again would have created a second row for one quantity, which is discrepancy class #1, arrived at by a rule intended to *improve* provenance. The rule also tripped its own >8 stop threshold purely on the duplicates. | **No** — caught before the write; nothing registered | **Clause added and applied at R2: a pin rule must exclude quantities that already carry a `number_id`.** With it the rule selects 6 directly and does not trip the threshold. Generalises: **any rule that adds rows to a registry needs an existence test before a merit test**, otherwise the merit test is doing duplicate-detection it was not designed for. Sibling of I-17 (multiple dated copies of one artefact) at the row level rather than the file level. |
| **I-37** | Provenance / numerals | IMPROVE | **The numeral 18 names three different objects in the reportable-plot / area space, and they are easy to swap.** (a) **18 unzoned reportable plots** — a *count*, = **31.6%** of the 57 reportable plots. (b) **18 zoned 14-day-grazing reportable plots** — a different count that happens to equal the first. (c) **18,561.5 ha** — `property_outside_mapped_ha`, an *area*. Caught at R1b when this session wrote "a fifth of the reportable plot set" (20%) for what is 31.6%, and the design seat read the error as a confusion with "the 18% unzoned share by area" — which is itself **35.7%** (`total_no_management_zone_ha` 30,711.6 / `TRUE_FARM_HA` 85,910.8), not 18%. So three readers reached for the same numeral and none of the three readings matched. | **No** — no deliverable number is wrong; the plot counts and the area pins are individually correct and registered | **Report only.** Rule: **whenever "18" appears near plots or unzoned country, state the unit and the denominator in the same sentence** — "18 of 57 reportable plots (31.6%)" or "18,561.5 ha outside the mapped census". The share-by-area figure (35.7%) is derivable from two pinned rows but is **not itself pinned**; pin it if it ever reaches a deliverable. Neighbour of C-07 (composite support_level) and the D8 two-floors pair — one appearance, several meanings. |
| **I-36** | Tests / reporting | **DEFECT — the summary string, not the test** | **`test_T8_headline_reproduction.py`'s exit line mixes two denominators and is genuinely ambiguous.** It prints `"{len(fails)} DRIFTED of {checked} checked"` — but `checked` counts **only** rows that have a recompute path, while `fails` counts **both** value drifts **and** rows with no path. The missing-path rows are therefore *not a subset of* the checked rows. Live: `"14 DRIFTED of 71 checked"`, which reads naturally as *57 of 71 reproduce (67%)* but actually means *71 of 85 pinned rows reproduce (84%), 14 have no derivation, 0 drift*. `14 + 71 = 85` = the pinned rows, which is the arithmetic that settles it. Two readers took the two different readings; register v3 recorded "57 of 71", a figure that appears nowhere in the data. | **Yes** — the coverage fraction goes on the pack's `How_we_know` sheet under a client cover, and the two readings are 67% vs 84% | **Diagnosed, not yet fixed.** Settled for now by `scripts/11_database/build_RPTSCOPE_reproduction_status.py`, which emits a per-row status table (`Output/tables/RPTSCOPE_reproduction_status.csv`, 88 rows: 71 REPRODUCES · 14 NO_DERIVATION_PATH · 3 NOT_PINNED_deliberate) and prints the four counts explicitly. **Any coverage number must come from that table, not from the summary string.** The fix in the test itself is a one-line change — separate `no_path` from `drift` and print all three counts — but it edits a green test during a delivery window, so it is post-deadline. **Rule meanwhile: never quote the exit string; quote the status table.** |
| **I-32** | Figures / acceptance | **DEFECT — class, not instance** | **Acceptance checks in this project test data objects, never rendered output, so a figure can pass every check while being wrong.** Worked case (Adrian pack T1, 31 Jul): `fmt()` used **`ifelse()`**, which returns a value the length of its **TEST** — the test (`sign`) was length 1, so the formatted number collapsed to a single element and **`sprintf()` recycled it across all four paddocks**. The figure rendered `45.3` in every column. The CSV was correct, and all five value-level acceptance checks passed, because they test the data frame and not the ink. **The PNG is what the client sees.** Generalises beyond `ifelse`: any length-1/length-n mismatch feeding `sprintf`/`paste` recycles silently, and R will not warn when the longer length is a multiple of the shorter. | **Yes, in the worst way** — a wrong number reaches a client deliverable while the registry, the CSV and the test suite all read clean | **Fixed for the class, not the instance.** New `R/gayini_assert_rendered.R`: `gayini_assert_rendered_values()` (each drawn string contains its source value), `..._varies()` (uniform strings where sources differ = recycling bug), `..._caption_number()` (a number quoted in prose matches its source at the precision the prose states), `..._table()`. **Standing rule: every figure that renders numbers as text asserts the rendered STRINGS against the source values.** Applied to the T1 table (values + ranks), F5 callouts, and the T13 Gate D community-SD caption line. **Guard proven to fire**: reintroducing the original `ifelse` in a fixture halts the build with `[T1 flood %] 3 of 4 rendered strings do not contain their source value`. Extend to any new numeric-label figure. **Two further members of the class, both found at T11 v2 (31 Jul), neither visible to any value check:** (a) **`theme_void()` + `ggsave` writes a TRANSPARENT PNG** — no `plot.background` is supplied, so the file renders as **black** in most viewers and takes black title text and legend labels with it. The Gate B residual map was unreadable and its title invisible while every number in it was correct. Fix: set `plot.background = element_rect(fill = "white", colour = NA)` on any `theme_void()` figure. (b) **A caption asserting a fill the figure does not contain** — the dual-grain subtitle promised "pale fill = not assessed" while the base layer was the union of the parts, so no pale ground could ever show. The inverse of the T13 white-fill defect: there, one appearance meant two things; here, a stated meaning had no appearance. **Both reach a client through a route no data-level check can see, which is the whole point of this issue.** |
| **I-31** | Registries / DB | IMPROVE | **No registry exists for a derived VECTOR build output.** `raster_asset` covers rasters, `census_asset` covers the parquet, and `spatial_layer_asset` is an **import** registry — it records layers read in from `Input/`, so a build-output row there is a category error (the same one CLAUDE.md already names for the `Gayini_Results.gpkg:management_zones` companion). Surfaced at T13 Gate E: `Output/spatial_8058/T13_part_polygons_epsg8058.gpkg` is the exact geometry behind a **registered** classification (`fact_zone_community_part_classification`, 115 parts) and its render-only twin, and neither can be registered anywhere without committing that category error. Both were therefore deliberately left unregistered. | **No** — no number changes. But the geometry behind a registered number currently has **no in-DB provenance row**: the chain is complete only via the exit bundle and the two tracked producing scripts | **Do not improvise a home.** Registering build outputs in `spatial_layer_asset` would corrupt an import registry and break `read_registered_layer()`'s contract. The fix is a new `vector_output_asset` table on the `raster_asset` pattern (path, layer, CRS, feature count, first-50-MB SHA-256, `path_exists`, `run_id`, producing script). **Post-deadline.** Meanwhile provenance lives in `Output/review_bundles/t13_paddock_part_classification.zip` and `build_T13_gateD_part_polygons.R` / `build_T13_gateD_render_geom.R` |
| **I-02** | Spine / T2 | **BLOCK** | Within-stratum `veg_p05` spread across the reference paddocks (fids 1–4) **exceeds the reference-vs-grazed contrast in 6 of 9 strata** — every Riverine and every Inland Floodplain band. Riverine-high: 36.5 pp within-reference (29.83→66.33) vs +8.25 pp pixel-weighted contrast (zone-support even smaller, +3.6/+0.1/−2.1). **A fixed distance-to-reference target is undefined in those strata.** Quantified at T2 Gate A (`v_zone_stratum_treatment_contrast` × per-fid `v_census_by_zone_stratum`). | **Yes** — T2 is the substrate for that metric | **Spine decision, not a build one.** The substrate is still correct to build (T2 proceeds). Then a spine-chat decision: narrow reference / per-stratum target / environmentally-defined (HCAS 3.3) / report heterogeneity as the finding. Do not work around it in the build |
| **I-03** | Task J | **EXTERNAL** | Jana email unsent — cut-date provenance (L07) and bank geometry (L10). Sole remaining blocker on Task J. | No — analysis complete | Send one email and proceed regardless, per `Gayini_path_to_Aug10.md`. Outstanding since ~18 July |
| **I-04** | T2 / S5 | **EXTERNAL** | `dim_management_zone.cropping_history` and four other RESERVED columns NULL pending Ernest's land-use table. So conserved-vs-**grazed** is available; conserved-vs-**formerly-cropped** is not. | Constrains the claim, not a number | Build what the data supports; name the gap in every caption. Graduate to limitations register |
| **I-05** | T3 | **EXTERNAL** | Adrian's LiDAR shrub-height model has not arrived. | No | T3 Gate E produces the overlay package so the overlay is a five-minute job on arrival |
| **I-06** | Spine §5 | DEFECT | S2 labels the plot-support 9 / 22 / 50 / 44 gradient "Support: stratum, pixel". The numbers are right; the label is wrong. `dim_metric.support` already stores both rules verbatim. | No — numbers correct | Spine-chat text fix. Also flagged for T4 Gate A |
| **I-07** | Spine §5 | DEFECT | C-3 states census `veg_p05` range as `[1.19, 97.00]`. 97.00 is the **`veg_p50`** max; p05 is `[1.19, 91.85]` all-pixel, `[1.19, 88.66]` non-treed. Conclusion (percent, no offset) stands. | No | Spine-chat text fix |
| **I-08** | T1 Gate D | DEFECT | `min_cell_n` flags on **pixel** count and is actively misleading. It flagged Aeolian-high (2,501 px) and Riverine-high (2,223 px) while **passing Riverine-low at 7,877 px — 93% one paddock.** The flag cleared the cell that produced the false headline. | No — the finding is now correct | The view still carries it and someone will trust it. Add a treatment-unit-count flag alongside, or annotate. Cheap |
| **I-09** | T1 Gate D | DEFECT | Aeolian Bala-only contrast is **1 paddock vs 1 paddock** (+21–24 pp). Must be reported as a paddock description, never as a contrast. | Yes if written up wrong | Wording constraint, recorded. Do not present as a treatment effect |
| **I-10** | Tooling (B5) | DEFECT | `run_spine_smoke_test.R:104-112` (`folder_scripts/archive_absent`) **hard-fails if `scripts/archive/` exists** — inverted against the archive convention it appears to enforce. A check that cannot fail. | No | Unresolved, Adrian's call. **Do not modify the smoke test to force it.** Use `lint_guardrails.py` exit 0 as the acceptance signal instead |
| **I-11** | Tooling | DEFECT | `run_spine_smoke_test.R` exits 1 on `structure/folder_scripts/10_downstream_optional` = missing, plus three `outputs` warnings. Pre-existing. **A permanently-red test gets ignored exactly like a permanently-green one.** | No | Same bucket as I-10. Post-deadline |
| **I-12** | Docs | DEFECT | Shipped data dictionary is behind the DB — `census_stratum` and `census_asset` absent from the `fields` sheet; `raster_assets` sheet has no `product` column. Recurrence of correction C5. | No | Regenerate from live DB. Post-deadline unless T4 runs |
| **I-13** | DB | DEFECT | `dim_metric.support` NULL on **36 of 45 rows** (populated for 7 pixel + 2 plot metrics). C10 leans on this column. | No | Treat unpopulated as unknown, never as either. Backfill where derivable — T5 Gate 4 |
| **I-14** | DB | DEFECT | `figure_asset` pre-Gate-E rows are an old-generation snapshot, unreconciled against disk. The 11 Gate E + 4 T1 + 2 Gate C rows are current. | No | Do not backfill. Reconcile post-deadline |
| **I-15** | Repo | DEFECT | `scripts/_deprecated/` violates the `scripts/archive/` convention — but cannot be reconciled without tripping I-10. | No | Blocked on I-10. Leave untouched |
| **I-16** | Output folder | DEFECT | 16 migration blockers remain (8 hard via `builder_csv_input`, 8 soft via `path_module_resolved` only). Two shadow figure registries feed nothing. | No | Explicitly parked before Aug 10 per `Gayini_path_to_Aug10.md` |
| **I-17** | Repo hygiene | DEFECT | Three dated contract snapshots in `docs/` (25, 26, 27 July). **Multiple dated copies of one artefact is discrepancy class #1** and has already misled four readers. | Yes, indirectly | Keep only the newest. Fold into the snapshot generator: delete predecessors on write |
| **I-18** | Process | IMPROVE | `T1_A_identity_margin.png` upper panel runs 0–0.15, compressing the band it exists to show into a thin strip. | No | Post-deadline, if the figure is ever regenerated |
| **I-19** | T5 | IMPROVE | Gates 2–4 deferred: QA verdicts → live views, `computed_at` / `v_qa_freshness`, `read_registered_layer()`, closed-ladder trigger, mixed-support detector. | No | Post-deadline. Gate 1 is done and has already caught a live violation |
| **I-20** | T4 | IMPROVE | Whole task deferred — `claim_register` and the spine evidence workbook. It is the durability artefact that stops C-1 recurring. | No | Post-deadline. Genuinely valuable; not an Aug 10 deliverable |
| **I-21** | T5 / tooling | IMPROVE | Add a `hardcoded_path` lint alongside `magic_number` / `or_ignore` / `whole_digest`. T2's Gate B/C scripts shipped session-scoped temp paths (UUID dir) that the numeric-only lint could not see; caught in review, not by the guard. A path lint (flag absolute paths / temp dirs / UUIDs in `scripts/` and `R/`) would have. | No | Post-deadline. Log now; do not build during T2 |
| **I-25** | T2 | IMPROVE | T2's per-**community** extraction (`fact_zone_community_veg_annual`) pools wetness bands, so the reference-vs-grazed gap decomposition carries a latent wetness confound. T6 showed the fix: extract by **stratum** (community × regime_band) controls wetness by construction (residual ≤3.3 pp vs a 26 pp gross skew). T2's two-arm decomposition was never re-cut at stratum grain. | Not for the deck | Post-deadline. Re-cut T2's gap decomposition at community × regime_band if the reference-vs-grazed floor number is ever used quantitatively. Not T6's job to fix; logged per Hugh's instruction |
| **I-24** | Process | IMPROVE | CLAUDE.md carries conventions that no acceptance gate checks (the review-bundle one was invisible until built — see C-12). Others may be equally unenforced (e.g. "one figure = one file = one slide", "every caption states support level", the review-bundle-per-task copy step). | No | Post-deadline. Audit CLAUDE.md conventions against the acceptance checklists / smoke test / lints; enforce or retire each |
| **I-26** | T12 / Task J | DEFECT | `Input/gayini_vectors_8058.gpkg` carries `irrigation_bank_cuts` (1,158 features) — a **Task J** input — that is registered in no `spatial_layer_asset` row. Found during T12 Gate A0 while registering the 8058 boundary/plots/community layers from the same GeoPackage. | No — outside T12's scope | **Log, do not fix** (per T12 spec Gate A0). Register under Task J when that trail is next touched, using the T1/T12 Gate A0 pattern. Not a T12 deliverable |
| **I-27** | Docs | DEFECT | CLAUDE.md "Current state" understates two counts: `spatial_layer_asset` **6→9** (T12 Gate A0, `spatial_007/008/009`) and `raster_asset` **126→166** (already-stale 126; +38 T12 Gate A). Stale prose vs data — the exact discrepancy class the provenance rules target. | No | **DONE at T12 close (28 Jul) → C-16.** Single CLAUDE.md edit applied: `spatial_layer_asset` 6→9, `raster_asset` 126→166, `figure_asset` 255→278, the T12 `dea_` objects, and the standing git rule (retires the branch/PR flow, I-28). |
| **I-28** | Process / git | — | **Standing git rule adopted (28 Jul 2026).** CC now: runs `git fetch/status/log` at session start and reports branch tracking + whether main moved; **pushes every gate** (`git push -u origin <branch>`, no approval, never batched/deferred, report the SHA); and **merges approved gates into main once per day** at end of day (fast-forward where possible), only for gates approved in chat. Never force-push, never rebase pushed work, never commit rasters/SQLite/large spatial data, no AI attribution — commits authored solely by Hugh. | No | **Deliberate relaxation of the human-merge convention.** Reason: the in-chat gate review is the substantive gate; the GitHub merge was ceremony. Backup (push) is decoupled from publication (merge). CLAUDE.md wording folded into the I-27 single close-out edit |
| **I-30** | Tooling / lint | DEFECT | **The magic-number lint blocks two spellings, not the class.** `MAGIC_LITERALS` bans the exact strings `0.0625` and `0.062351428`, so a *rounded-correct* bare literal for the 8058 pixel area slips the lint while still bypassing `gayini_params`. Three tracked scripts do exactly this: `scripts/05_ground_cover/03_h2_seasonal_gate_and_diagnostics.R:371` (`0.0623514`), `scripts/05_ground_cover/06_taskM_gateD_p05_ge80_contiguity.R:24` and `scripts/11_database/taskM_gateD_p05_distribution.py:53` (`0.0623512`). Values are correct-to-rounding (~0.0004% off, NOT the 0.238% `0.0625` error) and feed Task M diagnostics, **not** the 66 site / 21 paddock reports. Found in the T8 `0.0625` audit (`docs/change_reports/T8_pixel_area_constant_audit.md`). | No — no material area error; canonical areas route through `gayini_params` / `terra::res` | **Report only, not fixed** (per instruction — a fix in any report-adjacent path is routed via the report stream). Harden the lint to flag any bare decimal in `[0.062,0.063]` (and 30 m `0.09`) outside the param module — a T5 item, neighbour of I-21. |
| **I-29** | T10 / reference-state deck | **BLOCK** | The **five-period split** (1988–92 / 1993–2002 / 2003–12 / 2013–18 / 2019–22) — the deck's central §5 table (Reference ×4 gap −13.1 → −5.6; Bala 29ca −42.3 → −18.0) — is **produced by no script in the repo.** T10 Gate A exhaustive search (28 Jul, all 429 tracked text files): only Task J (2018-centred periods) and T12 (DEA sensor eras) carry ≥3 of the five boundary years; `T2_gateE_figures.R` writes a **two-window** report (early ≤1997 / late ≥2013, ±2 pp rule), not five periods. Independently confirms `Gayini_reference_state_methods.md` §8 ("the five-period split in §5 is not produced by any script"). | **Yes** — it is the deck's central table, and a figure nobody can reproduce is a number that doesn't reach anything | **Do not rebuild silently** (T10 Gate A instruction). The derivation must be located or rebuilt at **T10 Gate B** — a separate gated step, not this recon session. When rebuilt, reconcile against the design-seat regression (r=0.710, slope +0.548, grazed-median flood 28.6%) per T10 Gate B. |

| **I-33** | QA / render currency | **DEFECT — method, not instance** | **A file-timestamp test for render currency is a screening tool, and it fails in both directions.** AUD-1 flagged seven pack figures as Category G by comparing file `mtime` against an event calendar (the 28 Jul pins, the 31 Jul `floor_flood` precision correction, the 31 Jul QA-2a guard). REM-1 resolved all seven. **Six were false positives** — F1, F2, F4 (draw no affected number) and M5, M5b, F5 (read `floor_flood_*` from `dim_headline_number` at render time, so they were already current; the 62-second gap to the precision correction was harmless). Each was settled by scratch-rendering to a temp path and diffing: all six byte-IDENTICAL. **The seventh, F6, was real but invisible to the test that flagged it.** `T6_gateE_figures.R` queried `fact_three_arm_gap_decomposition WHERE regime_band='ALL'` — the rollup PIN 1 retired for drier-skew confound — and that rollup *still returns* the superseded values (−19.65 / −11.70 / +1.07). F6 was never a stale render; it was a **live render of a retired aggregation**, and it would have survived any re-render-based remediation byte-for-byte, looking corrected and being wrong. mtime says nothing about which rows a query selects. | **Yes** — the false-positive half wastes gate time on figures that are fine; the false-negative half is the class that reaches a client | **Screening only; never a verdict.** Two checks actually resolve currency, and neither involves a timestamp: (1) **render-and-diff** — re-render to a scratch path with `write_and_register_figure()` shimmed to a plain `ggsave`, and compare bytes; identical means current, on evidence, with no registry write; (2) **read the query behind every drawn number** — confirm the scope filter matches the pinned `scope_filter`, because a figure can be perfectly current with respect to time and still select a retired aggregation. QA-2b is rebuilt on this basis. The second check also answers whether each report-figure number traces to a nameable source, so the two collapse into one pass. **Successor work, found at REM-1 close:** `test_T8_headline_reproduction.py` reported `2 DRIFTED of 71` before REM-1 and **`14 DRIFTED of 71` after** — the two TaskU rows plus all twelve REM-1 rows, every one `recomputed=NOT RECOMPUTED`. Registration does not confer test coverage; nothing enforces the second step after the first. **Do not close it by copying the registration logic into the test** — CLAUDE.md requires an INDEPENDENT re-derivation, and a copied path would pass by construction and prove nothing, which is precisely the defect shape the test exists to catch. Needs a genuine second derivation per uncovered `number_id`. |
| **I-34** | Registries / DB | IMPROVE | **`table_asset` is not builder-integrated.** REM-1 Gate C created `table_asset` (the registry `Output/tables/` never had) on the existing registry pattern — same `path` / `product` / `run_id` / `path_exists` / `checksum_sha256` / `qa_status` / `superseded_flag` conventions as `raster_asset` and `report_asset` — and registered the two register-v2 pack items in it. **The builder does not create it.** A full builder re-run would destroy the table and both rows, exactly as it already would the 9 EPSG:8058 Task H rasters, `census_asset` and `dim_metric.support`. | **No, while the standing rule holds** — the builder is never re-run, and no non-destructive registration strategy exists yet | **State it, do not fix it here.** `table_asset` joins the list of post-build manual registrations under CLAUDE.md **Database**; add its creation + registration script to that sequence when the non-destructive registration strategy is built. Precedent worth noting: this is the same shape as **I-31**'s proposed `vector_output_asset`, and `table_asset` is now the working example of that pattern — build both together. |
| **I-35** | Provenance / Output | IMPROVE | **`Output/tables/` is an entire output class that was outside provenance.** At AUD-1's Gate A snapshot it held 83 files and **no registry covered any of them**; it now holds 93 (TaskU added tables during the audit) of which **2 are registered** — the two register-v2 pack items, done at REM-1 Gate C. The remaining 91 include the T13 sweep and robustness tables, the T6 in-scope point sets, the taskM diagnostics and every taskU Gate U1/U2 output. This is structural, not a one-off: results tables had nowhere to be registered, so none were. | **Not before 10 August** — the only two tables named by a pack document are now registered, and unregistered tables block nothing else that ships | **Do not bulk-register.** A sweep would stamp provenance on files nobody has checked, which is worse than an honest gap: `run_id` and `product` have to be established per table, not guessed. Post-10-August job, sequenced after **I-34** so the registry is builder-integrated before it is filled. Until then `table_asset` row count is a deliberate 2, not a partial migration. |

## Closed

| ID | Issue | Resolution |
|---|---|---|
| **C-01** | `plot_rs_analysis_base.csv` reported missing, blocking script `05` | **Present** at `Output/csv/canonical/` — gitignored, not missing. Script 05 can run |
| **C-02** | Reference set — three or four conserved paddocks? | **Four.** Fids 1–4 (Bala 26ca/27ca/28ca/29ca), all `zone_group = 'Bala'`, `grazing_excluded = 1`. The "three" was a miscount, confirmed from the DB |
| **C-03** | `raster_asset` extents unpopulated ("98 of 98") | **Stale QA row dated 2026-07-01.** All 126 rows populated. Misled four readers including the spec author, twice |
| **C-04** | `Area_MW` / `ManagmentZ` do not exist | **They do** — in `management_zones_epsg8058.gpkg`, capitalised ESRI names. The spec had inspected the wrong layer. Three management-zone objects exist, not two |
| **C-05** | Zone identity unverifiable (27 of 64) | **All 64 verified.** Provenance + 62/64 assignment closure. `{9,21}` are area-twins held by provenance alone — recorded as `provenance_only` |
| **C-06** | Refugia "~4,300 ha" — three competing lineages | **Withdrawn** by Task M (D8). `green_at_floor()` measures green *share*, not total cover. The grid mismatch explained only the 6,458 ↔ 4,474 pair |
| **C-07** | Composite `support_level` | Split into closed-ladder `support_level` + free-text `aggregation_unit`. **Specced four times before it was caught** — logged as a method trap in lineage §9 |
| **C-08** | The 0.0625 pixel-area error | `gayini_params.PIXEL_AREA_HA` derived, never typed; magic-number lint fails on a bare literal. Caught a stray literal in its own author's script within hours |
| **C-09** | `OR IGNORE` passing a stability test while the DB is wrong | `INSERT OR REPLACE` enforced by lint; idempotence now tested by **convergence** |
| **C-12** | Review-bundle convention was in CLAUDE.md but in no acceptance checklist (was I-23) | **Mechanised.** `scripts/utils/build_review_bundle.py` (spec-driven) now produces `Output/review_bundles/<tier>_<task>/` + zip + MANIFEST (scripts w/ git status, DB objects w/ row counts + CREATE SQL, registry ids + checksums, artefact sizes, deletions, and every acceptance criterion with its number). Run for T2 (`docs/review_bundles/T2_zone_annual_veg_spec.json`). Bundle 1.6 MB; >10 MB artefacts referenced by path+checksum, not copied |
| **C-11** | D2 site dashboards "5 of 66" — 9 unbuilt (was I-22) | **57 of 66 is the deliverable.** The 9 unbuilt are exactly the `ground_cover_exclusion_flag = 1` treed Floodplain Woodland/Forest plots — the context/treed set the project excludes from ground-cover interpretation. **Decision (Hugh, 27 Jul): leave unbuilt** — building them would need a ground-cover-free variant for a set that is deliberately set aside. 57 = the analyzable plots. The composer's `limits length 1` error (`gayini_dashboard_panels.R:54`) stands unfixed by design; do not build the 9 |
| **C-10** | FC values >100 in the annual veg stacks (max 108 mean / 110 jja_son) — unmixing overshoot or water contamination? (was I-01) | **Bilinear-resampling overshoot**, traced to `legend_semantics` ("bilinear" to the 8058 grid). Only **24 over-100 pixel-years of 143.1M valid (0.00002%)** in the primary, 622/133.95M (0.0005%) in jja_son. The 3.5× wet-enrichment is on n=24 — noise; real water contamination would be thousands-to-millions of wet-year pixels. **Keep raw, no mask, no clamp**; add `n_px_over_100`/`pct_px_over_100` counters; tolerance stated from the FC product domain [0,100], not the observed range |
| **C-13** | Spec identifier collision — two artefacts numbered **T7** (the DEA Land Cover extraction, and "T7 — Persistence surface" in `Gayini_reference_state_specs_T7_T11.md`). Class: **duplicate identifier** — discrepancy class #1 territory. Flagged at T12 Gate 0 recon. | **Resolved by renumbering the DEA task T7 → T12** at spec v2. `docs/reference_update/T7_dea_landcover_l3_extraction.md` archived to `docs/archive/` with a superseded-by header (not deleted); `T12_...md` v2 is the live spec. No number was ever produced under the ambiguous identifier |
| **C-14** | T12 §2.4 v1 `dea_indeterminate` rule ("< 5 valid years in the era") made the **2019–2022 era indeterminate by construction** — that era is only four calendar years long, so it could never reach five. A defect in the rule, not the data. | **Corrected at spec v2**: "< 4 valid years **or** < 60% of the era's calendar years present"; floor window 2023–2025 exempt (calibration input, never classified). **Caught before any number existed** — the classification gate (Gate D) has not run |
| **C-16** | T12 close-out (was I-27 doc-sync + Gate B farm/community decision) | **T12 closed as a documented negative** (spec v4 §2.7 fired). CLAUDE.md single edit applied (I-27 done). **Gate B farm/community as separate `dea_`-prefixed tables: confirmed by Hugh, no rework.** 42 zone-era false positives recorded (§2.8: never call them cultivated); `cropping_history` NULL 64/64 is now a finding (DEA cannot fill it), not a gap. Methods slide (sensor-era gap), 2 maps, spine return (S5 gains no land-use variable), limitations rows L-T12-a/b/c staged. Next: 66 site reports |
| **C-15** | Refugia branch `fix-refugia-changelog` — should it merge to main? | **Deleted deliberately (local + remote, 28 Jul), unmerged.** Merging would have **regressed** main: main's `Gayini_established_data_facts.md` quick-facts row 5 already carries the discipline-correct, **number-free** wording deferring to `Output/tables/taskM_green_at_floor_area.csv`; the branch hardcoded "~6,460 ha supersedes ~4,300 ha". Commit `e87bd1a` survives as a git object if ever needed. **Canonical number, read from that CSV this session: `area_ha_native_30m_3577 = 6,457.95 ha`** (majority-green floor = `green_frac_pct = 100·PV/total_veg > 50`, paired at each pixel's total-veg p05 season; native 30 m / EPSG:3577; 959,833 valid px; 7.476%). This ≈ "~6,460 ha" and **does reproduce** from the native `green_at_floor()` route (`scripts/05_ground_cover/04_taskM_green_at_floor_area.R`). **Correction to the chat framing:** the `Tier2_TaskM_deck_evidence_audit_v2` "unverified" flag was specifically about the census-**parquet** route (parquet holds `veg_p05` = total cover, a *different variable* = 40,935.8 ha) and about the number floating in prose without citing its artefact — **not** about the value being unreproducible. So ~6,460 was the right value; it needed to cite this CSV. `~4,300 ha` remains withdrawn (C-06 / D8). Five group-A docs bannered (not rewritten — the audit trail is preserved); groups B/C left alone |

---

## Task U — LiDAR structural lens (1–2 Aug 2026)

*ID prefix `U-I` checked against this log before use: no collision with the `I-`/`C-`
series. Full detail in `docs/change_reports/TaskU_gate*.md`.*

| Id | Item | Triage |
|---|---|---|
| **U-I11** | **A validity test that silently returned the permissive answer instead of erroring.** `valid_of()` tested `isinstance(nodata, float)`, which is False for `np.float32(nan)`, so the NaN branch never fired, the test fell through to `a != nan` — true everywhere — and the `d5` tile never entered the 5 m mosaic. **Same class as the stored QA verdict (C-08 territory) and `OR IGNORE` (C-09): it looks like it worked.** | **CHANGED NUMBERS.** Fixed 2 Aug; U1/U3/U-Q4a re-run. Found because 2021 height coverage read 51,167 ha — exactly the `d4`-only figure, a number matching another it had no business matching. Blast radius established **by re-running, not reasoning**: the 10 m and 50 cm paths reproduce to the decimal |
| **U-I14** | **Bala 26ca, Riverine — a signal seen twice, independently.** R6 residual **−17.41 pp** (n = 636); U-Q4a makes it structurally poorest of the four, p90 = 0.00, 0th percentile, **at both epochs**. Two products, same paddock, same community, same direction; both small samples, neither built on | **OPEN, named, not investigated.** Deliberately not chased before 10 Aug. Recorded so it is picked up on purpose rather than rediscovered |
| U-I7 | Gate U3 item 1's suggested stable-ground derivation (persistently-zero FPC at both epochs) is **circular for the FPC test** — it forces the difference to zero by construction. Landsat-based derivation substituted | Would have manufactured a false "no sensor offset" pass. Recorded so it is not reintroduced |
| U-I9 | LiDAR `bbh` has **13.33% dynamic range** on-property. Promoted to a finding (S6) as well as a limitation | **To the limitations register.** Any future task scoping LiDAR FPC at Gayini needs this first |
| U-I10 | **There is no stable vegetated ground on Gayini between 2009 and 2021.** A property of the delivery, not a method defect; Gate U2 predicted it | **To the limitations register** as a limitation of the two-epoch LiDAR comparison |
| U-I13 | No stable-ground pixels fall inside the `d4`/`d5` seam, so the seam's vertical behaviour is untested | Matters only if a planar de-trend is ever built (post-10 Aug) |
| U-I1 | `read_registered_layer()` is mandated by CLAUDE.md and referenced in three script headers and four docs but **is defined nowhere in the repo**. Its contract was implemented inline in the Task U scripts | IMPROVE. The convention is currently unenforceable |
| U-I8 | No roads / hardstand / infrastructure layer exists in the repo; stable ground had to be proxied from Landsat persistence | Limits interpretation; changes no number |
| U-I12 | `dim_headline_number` grew 76 → 88 between two Task U registrations. Traced to `rem1_rerender_20260801` (`register_REM1_three_arm_community_pins.py`), another workstream | Benign. Recorded so the jump is not re-investigated |
| U-I15 | R2's 2021 exclusion moved 0 → 1 px after U-I11, so its 3× STOP condition now fires at ratio **218** rather than ∞ | No. Design-seat clearance stands on the substance (0.545 ha vs 0.003 ha). Recorded so the amended number is not read as a reopened decision |

---

## T3 — persistent vegetation surfaces (3 Aug 2026)

*ID prefix `T3-I` checked against this log before use: no collision with the `I-`/`C-`/`U-I` series. Full detail in `docs/change_reports/T3_gate*.md`.*

| Id | Item | Triage |
|---|---|---|
| **T3-I1** | **Reprojecting a thresholded surface is not the same operation as thresholding a reprojected one — log this as a general rule, not a T3 note.** Bilinear-then-threshold and threshold-then-bilinear differ by **16%** on the green-share floor (3,744.20 ha vs the 4,474.03 ha count conversion), and the reprojected figure is **not even stable under scope** (3,363.36 ha over the census footprint). **Any future reprojection of a thresholded surface must declare the order.** The measured area is always the one on the grid where the metric is defined. | **STANDING RULE.** The most reusable thing Gate B2 produced. This is the general form of D8 |
| **T3-I2** | **`veg_p05 IS NULL` matches 0 of 1,080,157 rows.** The census percentile columns encode missing as **float NaN**, not SQL NULL, so an `IS NULL` filter silently admits all 155 NaN pixels and drags MIN/MAX/AVG to NaN. Correct predicate: `(veg_p05 IS NULL OR isnan(veg_p05))`. **Audited 3 Aug: no contamination.** SQLite cannot store NaN (the Python driver converts it to NULL — verified), so every `IS NULL` on a SQLite REAL column is correct by construction; four live fact tables show 0 non-finite among non-NULL. The only parquet/duckdb site, `taskM_gateD_p05_distribution.py:60,71`, already pairs `IS NOT NULL` with `isfinite()`. | **KNOWN-CLEAN, rule recorded.** The trap is confined to the parquet path. The *next* parquet query is the one at risk |
| **T3-I3** | **Two registered files over 50 MB carry whole-file SHA-256 against the first-50-MB convention** — `raster_total_veg_annual_mean_8058` (609.2 MB) and `raster_total_veg_annual_jjason_8058` (579.6 MB), both from T2. A canonical verifier will report drift on unchanged files. The other five >50 MB assets (2 FC stacks, 2 Task U DEMs, the gauge sqlite) are correct. Latent source: `02_build_total_veg_percentile_rasters.R:623-626` uses `digest::digest(file=)`. | **KNOWN-DIRTY, 2 rows.** Fix is a two-row UPDATE; **not applied** — rewriting a registered checksum is a provenance act and wants sign-off |
| T3-I4 | **The GeoPackage silently disagreed with its own raster.** Components are labelled 8-connectivity (queen); polygonising a boolean and casting MULTIPOLYGON→POLYGON splits at every diagonal pinch, i.e. reverts to rook. 40 green-share components became 26 polygons and **219 ha vanished** when sub-parts fell back under the 5 ha filter. Fixed by polygonising a **component-id raster** and dissolving by id; all four layers now reconcile to 0.01 ha. | **CHANGED A NUMBER in a client-facing deliverable.** Adrian overlays the GeoPackage, so this would have been an error he inherited |
| T3-I5 | **No channel or watercourse layer exists anywhere in the project** — not in `spatial_layer_asset` (9 rows), not under `Input/`. The only hydrological geometry is `irrigation_bank_cuts` (1,158 points), which is Task J **irrigation infrastructure, not natural channel**. Gate E's distance-to-channel is therefore NULL and flood frequency is used as a proxy. | **BLOCKS a real test.** The channel-association result is proxy-based until a channel layer is sourced. Substituting the bank cuts would be a category error |
| T3-I6 | FC intermediates (`fc_{total_veg,pv}_3577_wy1988_2023.tif`) carry **no EPSG authority tag**: `crs(x, describe=TRUE)$code` = NA (name `GDA94_Australian_Albers`) though `same.crs(x, "EPSG:3577")` = TRUE. A reader testing the code would conclude the CRS is unknown. | Cosmetic. Changes no number |
| T3-I7 | `taskM_gateD_p05_distribution.py` types `pixel_area_ha=0.0623512`, a 7-sig-fig rounding of `PIXEL_AREA_HA`. Relative error 1.3e-7, immaterial — but it **evades the magic-number lint**, which matches only the exact string `0.062351428`. The lint catches the literal, not the class. | IMPROVE. Suggests the lint should match rounded variants too |

---

## Notes on discipline

**What I will stop doing** (design seat): raising `IMPROVE` items at STOP points. They arrive looking as urgent as blockers because of where they appear. From here they go straight to this log without interrupting a gate.

**What CC should keep doing:** flagging rather than choosing. Every significant error in this project was caught at a STOP — the wrong gpkg, the twin degeneracy, the Bala confound, the >100 values. The flagging is not the drag; the drag was treating every flag as equally urgent.

**Cadence:** review `BLOCK` and `EXTERNAL` at each task boundary. Everything else weekly.

**ID hygiene:** a proposed issue ID must be checked against the on-disk log before use — never assigned from memory or a prior chat. (I-21/I-22 were mis-referenced from memory twice; the on-disk assignments — I-21 `hardcoded_path`, I-22→C-11 dashboards — are authoritative. The renumbered rows above, C-12 and I-24, took the next free IDs.)
