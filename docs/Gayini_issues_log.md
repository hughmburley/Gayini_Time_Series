# Gayini — issues log

**Started:** 27 July 2026 · **Deadline:** 10 August 2026
**Purpose:** capture build and process issues without letting them stop delivery.
**Scope:** build, process, tooling and paper-trail defects. **Scientific limitations belong in `Gayini_limitations_register_*.xlsx`** (currently v10, 43 rows). A row here graduates to that register if it turns out to constrain what the science can claim.

---

## The triage rule

At any gate, one question decides whether an issue stops work:

> **Does this change a number that reaches a deliverable?**

- **Yes → resolve before the gate closes.** A wrong number is more expensive later than the delay is now.
- **No → log it, state the disposition, proceed.**

That is the whole rule. It is deliberately mechanical so it can be applied in seconds without debate.

Two corollaries, both learned the hard way:

- **"Would be better" is not "is wrong."** Improvements raised at a STOP point read as urgent because of where they appear. They are not. Log them as `IMPROVE` and move on.
- **A number reaching a deliverable includes the paper trail for that number.** A figure nobody can reproduce is a number that doesn't reach anything.

### Severity

| | Meaning | Action |
|---|---|---|
| **BLOCK** | Wrong output if ignored | Resolve now |
| **DEFECT** | Real, but current output is correct | Log, fix when cheap |
| **IMPROVE** | Would be better | Log, revisit post-deadline |
| **EXTERNAL** | Waiting on someone else | Log, chase, work around |

### Review cadence

At each task boundary (not each gate): scan `BLOCK` and `EXTERNAL` only. Full review once weekly and once post-deadline.

---

## Open

| ID | Area | Sev | Issue | Affects a deliverable number? | Disposition |
|---|---|---|---|---|---|
| **I-38** | Provenance / aggregation | **DEFECT — mechanism, not instance** | **`aggregation_order` is the mechanism behind the historical number discrepancies, and it is invisible in a value.** Worked case, R2 prep: the register's post/pre inundation pair **43.6% / 22.8%** did **not** reproduce as mean-of-paddock-means, which gives **46.0% / 25.2%** — a 2.4 pp gap on both terms with *identical* inputs, filters, period and support. It reproduces **exactly** as pixel-weighted `SUM(wet_pixels)/SUM(valid_pixels)`. The n's (4 post, 31 pre) matched under both, so the usual tell — a wrong denominator — was absent. Two defensible aggregations of one quantity differ by more than most of the twelve discrepancies the T1 audit catalogued. | **Yes** — 43.6/22.8 is in the pack's Q7 answer cell, and the same ambiguity sits under every paddock-to-property rollup | **`dim_headline_number.aggregation_order` already exists as a column and is the fix — it is under-used, not missing.** Standing rule: **any number that rolls paddocks up to the property states its aggregation order in the same breath as its value**, because the value alone cannot distinguish them. Every pin registered at R2 carries the column populated. Neighbour of C-07 (composite support_level) and the two-floors pair — one name, several quantities. |
| **I-47** | Tests / standing rule | **STANDING RULE** · **CANDIDATE ROW for pack item T3's next revision** (Ruling AK — alongside [[I-46]]; the client page is **not** reopened) | **A CHECK MUST NOT MATCH THE RECORD OF A CORRECTION AS THOUGH IT WERE THE ERROR.** Where a convention writes corrections **visibly**, a fragment match will find the quoted old value **inside the note that documents its replacement**. **Match the whole claim, not a substring of it. The note is the record; the sentence is the claim.** Worked case, 4 Aug (LID-1 Ruling AG): fourteen `legend_semantics` rows were corrected from *"0 px in 2021"* to *"1 px / 0.0025 ha in 2021"*, each gaining a note quoting the old value. The verification queried `legend_semantics LIKE '%0 px in 2021%'` and reported **14 rows still stale over data that was already correct**. | **No number changed** — and that is the point. A false positive on a verification is as expensive as a false negative: it invites a second 'fix' to data that was never wrong, and the second fix is the one that breaks something | **THE DATA WAS RIGHT THROUGHOUT, ONLY THE CHECK WASN'T.** This is **Ruling J's distinction arriving from the other direction**: J says a check that ERRORS is not a check that CATCHES; this says **a check that FIRES is not necessarily a check that FOUND something.** The rule belongs beside the visible-correction convention because **the convention generates this failure mode by construction** — and this project has made visible correction standard practice: register v3's §5 block, the superseding headers in both stream documents ([[I-40]] ninth instance), and the fourteen semantics notes. Fixed by matching the whole sentence and converging on a re-run rather than failing. |
| **I-48** | Process / provenance | **DEFECT** | **A governing document named in a spec was not in the repository when the spec was issued.** `Gayini_report_stream_handoff_20260804.md` is absent from `docs/`, `docs/reports/` and `docs/reference_update/`. DOC-3's C4 prior evidence and C12's standing rule were both taken from the spec's quotation of it. **Second occurrence in three tasks** — the DOC-1 spec was also absent until it was committed mid-task. A spec quoting a document nobody can open has **no check against transcription error**, and both seats reasoned from that handoff for a full session. | **Indirectly** — the quoted content set the disposition on C12, which reached Ruling U | **Commit `Gayini_report_stream_handoff_20260804.md` and the DOC-1 spec to `docs/reference_update/`.** **STANDING RULE: a document named in a spec must be in the repository at the time the spec is issued.** Same family as [[I-40]] — the record exists but is not where the reader is sent. |
| **I-49** | Process / concurrency | **DEFECT** | **The second worktree is a live session, not an abandoned directory.** `D:/Github_repos/Gayini_reports` on `feature/reports` carries commit `48e8ada` at 20:12 on 4 Aug — *"REPORTS v1.4: rulings R-1 to R-5 applied; canaries now proven able to fail"* — plus **two uncommitted files**, `scripts/15_reports/report_build.js` and `scripts/15_reports/report_data.py`. It is working on the report build, the same subsystem Ruling U-a would have touched. Cross-reference [[D1-I1]]: four collision incidents, and the collision is at commit time, not at staging. | **No** — but it would have, had U-a been executed against a held worktree | **Resolved on sight for the holder question.** **One item does not close with it: the design seat does not hold rulings R-1 to R-5**, which that commit records as applied. The report batch and the methods document must agree on the same numbers, and rulings T, V, W, X, Y and Z were issued against the same material on the same day. **Request the R-series rulings before either stream applies further text changes.** |
| **I-50** | Provenance | **DEFECT** | **The 78 registered dashboard renders are not reproducible from head.** All of them predate the last commit to every file that produces them — `gayini_dashboard_panels.R`, `gayini_dashboard_compose.R` and `gayini_veg_water_census_panels.R` were all last committed in `5d78ce0`, 23 Jul 14:28, against renders dated **D1 23 Jul 12:09** and **D2 20 Jul**. `R/gayini_veg_water_census_panels.R`, which supplies the **response panel of every dashboard**, carries an mtime of **23 Jul 12:59** — after the D1 renders and eleven days after the D2 batch. **A re-render today would reproduce neither set byte-for-byte, and whether the difference is cosmetic is unestablished.** | **No** — but it affects the paper trail for **78 figures** registered at `qa_status = REVIEW` and shipped into the methods document | **Post-deadline: re-render, diff, and either re-register or record the drift.** **Before 10 August: one sentence in V10's front matter noting that the dashboard figures predate the current state of their producing code.** Same family as the four unreproducible figures already declared there ([[D1-I6]]). Independent of the p-value question and the more consequential of the two. |
| **I-51** | Process / divergence | **RECORDED DIVERGENCE** | **The dashboards retain a p-value against the report-stream handoff §4 instruction to drop it.** Ruling U-b, 4 Aug: `show_kw` is not changed and no dashboard is re-rendered before 10 August. Recorded rather than left implicit, per the ruling. **The rule's purpose is served** — the pack carries no p-value, the 57 site reports crop it out at `report_figs.py:432` (crop ends at 60% of figure height; the caption sits at ≈74%), and the 21 paddock reports neither read the dashboards nor compute one. **The only carrier is the methods document**, at Figures 12, 13 and 14, where §6.6 states the test, its grain, its four-class grouping including the woodland context class, and why it is labelled descriptive. | **No** — no client-facing document carries a p-value | **Divergence accepted on the record.** Ruling U's stated trigger (a radius wider than the dashboard family) was **set aside as aimed at the wrong risk**: what governs is whether the re-render is reproducible, and per [[I-50]] it is not. **Post-deadline: re-render deliberately, diff, establish whether the response-panel drift is cosmetic, and decide `show_kw` on a clean base.** |
| **I-52** | Provenance / naming | **DEFECT — class, not instance** | **One name over two counting bases: `n_units` counts geometry, the part classification counts support.** `fact_three_arm_gap_decomposition.n_units` gives Aeolian conserved as **2** — Bala 28ca and Bala 29ca, both holding Aeolian pixels. The part classification gives **1**, because Bala 28ca's Aeolian portion is **10 px / 0.62 ha** and is one of the three parts failing the ≥30-valid-cell support rule (with Bala 15 · Riverine at 23 px and Mara 3 · Aeolian at 1 px). **Both counts are correct and they answer different questions.** Figure 26's label carries both — `n=2` with a parenthetical `n=1: Bala 29ca` — with no key. | **No.** The fragment is **0.08%** of the arm's Aeolian pixels, so no pinned value moves. It affects every place both counts are quoted | **Third instance today of one name covering two bases** — flood-frequency footprints (Ruling T), aggregation orders ([[I-38]], Ruling AA), now geometry against support. The T3 page already lists *"one numeral, several quantities"* as a documented failure class with six historical instances; today added three. **Ruling AB §4.6(c) is the control: whichever counting basis is used is named.** Registry reconciliation of `n_units` is **post-deadline**. Verification: `Output/audit/DOC3_refstate_figure_verification.md` §3. |
| **I-53** | Tests / standing rule | **STANDING RULE** | **A GATE IMPLEMENTED BY REASSIGNING A FUNCTION IS NOT A GATE.** Ruling AC required a scratch render with no registry write. The driver sourced `R/gayini_figure_register.R`, then overrode `gayini_write_and_register_figure` in `globalenv` to redirect to scratch. **Each producer sources that helper itself**, which reassigned the real function *after* the override, and the real path ran: six figures re-rendered in place and **six `figure_asset` rows written**. The override was defeated by the thing it was overriding. **The fix that works removes the capability rather than shadowing it** — read the producer's own text, drop only the `source(...)` line that restores the helper, and eval the rest, so the real function cannot exist in the session. | **No number changed** — the renders were correct and both assert suites passed. It moved six registry rows without authority | **Third instance of one family.** Beside *"a criterion stated as a typed literal is not a check"* and Ruling J's *"a check that ERRORS is not a check that CATCHES"*: **an override is a preference, not a constraint, wherever the overridden thing can reinstate itself.** Test for it by asserting the guard still holds *after* the guarded code has run, not before. **Second worked case, same day (Ruling AG 3).** `scale_x_continuous(limits = c(1988, 2023))` was set to reserve a label gutter. A scale `limits` argument **drops** data outside the range rather than zooming, so it silently deleted **every direct label it had been added to make room for** — the opposite of its purpose, with no error. `coord_cartesian(xlim = ...)` zooms without discarding and is the correct tool. **Both instances were caught by looking at the artefact, not by a check.** **General form: a guard or constraint that can silently DISCARD rather than FAIL must have its effect asserted on the output, never its intent asserted on the input.** **Third worked case, same day (PACK-1 v1.2).** `PACK1_final_number_check` was extended to scan the pack captions. `resolve()` consults the `PINS` table **only through the `ids` passed in**, so the first attempt — scanning with `contract=False` and an empty id list — **could not match a pin at all**, and reported 24 registered numbers as unresolved. The check ran, produced output, and had examined nothing it was pointed at. Fixed by giving each caption its own `number_id` contract. **The three instances span an R function reassignment, a ggplot scale argument and a resolver contract — the pattern is not language- or library-specific.** **General form, stated: a check that can return clean without having examined anything is indistinguishable from a check that passed, and the only defence is asserting the check's own COVERAGE — how many things it looked at — alongside its verdict.** **Fourth worked case (V13 build, 6 Aug), and it EXTENDS the family from checks to mutations.** `build_v13.py` updated the version stamp by string replacement. It **matched the wrong string, replaced nothing, and reported success** — the stamp still read *Draft v11.2* in a document numbered v13. Found by rendering the document, not by any check; it had passed schema validation, as had two other defects in the same build. **The first three instances are GUARDS that returned clean without examining anything. This one is an EDIT that returned clean without changing anything** — the same indistinguishability, on the write side rather than the read side. **A replace that matches nothing and a replace that succeeds are identical in their return.** Fixed by matching on a pattern and asserting the post-condition. **Fifth worked case, found by that very fix (6 Aug).** The corrected producer matched `Draft v[0-9.]+` and so repaired the version — and **stopped one character short of the date beside it, in the same string, in the same replace**. The stamp read *Draft v13 · 4 August 2026*, a date carried stale through v11.1, v11.2, v12 and v13. **Case four matched nothing and reported nothing; case five matched something and reported success while leaving the adjacent field wrong. A PARTIAL match reads exactly like a COMPLETE one.** **General form: a pattern that matches part of a compound field silently asserts that the rest is correct.** Fixed at the producer by matching the whole compound field, not the file. **The property, not the incidents:** all three defects in the V13 build passed schema validation and all three were found by RENDERING the document. The check that existed caught none of them. That is this family stated as a property of the toolchain rather than as a run of accidents — and it is why the defence is asserting the post-condition on the artefact, never the intent on the input. Recovery: Rulings AD and AH. |
| **I-54** | Process / audit scope | **DEFECT** | **AN ARTEFACT GENERATED BY A BUILDER THAT NO AUDIT IS SCOPED TO IS INVISIBLE TO EVERY AUDIT.** The methods document's **numbered captions** are emitted by the document builder. They are not body text, so DOC-1/2/3's text passes never read them; they are not figure-producing code, so the producer review never read them either. **They fell between two scopes that each looked complete.** That is how *"the interquartile band and median of the 60 grazed paddocks"* — whole-paddock language beneath a **part-grain** figure — survived DOC-1, DOC-2, DOC-3 and REM-1. Same class as C4: the words name a different unit from the thing drawn. | **No number moved**, but it concealed a **grain error in a client-facing caption** through four audits | **Remedy is a build-time assertion, not another audit gate** — now in `build_v11.py`, scoped to the caption paragraph, failing the build on barred vocabulary or a missing Ruling T footprint. It caught a false positive on first run (a 900-character window read *"reference set"* from Figure 28's body, which is the concept and not the barred label) and was narrowed. 28 captions, PASS. **The same gap existed in the pack:** `PACK1_final_number_check` covered the workbook sheets and the what-we-dont-know page but **not the item-list captions**, so every number written in a pack caption was outside it — closed at v1.2, each caption now carrying its own `number_id` contract. **Cross-reference [[I-53]]:** same family — the check has to run on the output, not on the intent. |
| **I-55** | Provenance / single source | **DEFECT** | **A fourth place a caption can disagree with the item list, outside the assertion that closed [[I-54]].** `00_START_HERE.md` and the workbook Contents sheet both draw their one-line *"what it answers"* string from a **`WHAT` dict hardcoded in `scripts/13_pack/PACK1_build_workbook.py`**, not from `PACK1_item_list.csv`. The builder's own docstring says the two generated documents read one source so they cannot disagree - true of item, filename and order, and **not true of the caption line**. The v1.2 assertion scans the item-list `caption` column and does not see the `WHAT` strings at all. | **No.** The three agree today because they were checked by hand at v1.2 | **Post-delivery.** Move `WHAT` into `PACK1_item_list.csv` as an `answers` column so the generated documents and the assertion share one source. Same class as [[I-54]]: an artefact no check is scoped to. Logged with an ID rather than carried as a note, because a note is exactly what I-54 shows does not survive. |
| **I-56** | Tests / standing rule | **DEFECT — method, not instance** | **RESOLUTION BY VALUE IS NOT ATTRIBUTION.** FIGSEQ Part B resolved every number in V12's 28 captions and 46 figure passages by matching each value against every `pinned_value` in `dim_headline_number`. **Of 98 hits, 51 matched MORE THAN ONE pin** — *"3"* matches ten, *"10"* and *"10%"* match six each, *"5"* and *"5th"* five. A value-match establishes that a number **exists somewhere in the registry**, not that the caption **took it from there**, and a coincidence is indistinguishable from a citation. **The reported count of 98 registered is therefore softer than it reads; the adjudicated findings, which were resolved individually, are not.** | **No.** No wrong number was reported and none was missed — the 47 unmatched were adjudicated by hand and all reproduced. It overstates the strength of the 98 | **Remedy is the pack's, already proven at PACK-1 v1.2: each caption carries its own `number_id` contract, so `resolve()` matches by IDENTITY rather than by value.** Post-deadline. **Cross-reference [[I-53]] — same family: a check that cannot distinguish the right answer from a coincidence is not yet a check.** Where I-53's instances returned clean without examining anything, this one examines everything and cannot tell attribution from arithmetic accident. The defence is the same: assert what the check actually established, not what it appears to have established. |
| **I-57** | Figures / report stream | **DEFECT — latent, live fuse** | **A FORMATTER THAT ONLY WORKS ON THE DATA IT HAPPENED TO MEET.** `docs/reports/Gayini_Paddock_reports_V2/report_figs.py:132` writes the trend annotation as `f'trend +{sl:.3f} pp a year, read from the registry'` — **the plus is hardcoded, so the sign comes from the format string and not from the value.** It has never misprinted because the only registered slope reaching this path is Bala 29ca's and it is positive. **The fuse is already in the registry:** of the eight Bala 29ca trend rows in `dim_headline_number`, `t10_bala29ca_inland_floor_trend` is **−0.216** — so the annotation is one argument away from printing `+−0.216` on a client-facing page. | **No — no shipped page is wrong.** It becomes wrong on a re-pin, a scope change, or the next paddock routed through this producer | **The fix belongs to the REPORT STREAM, not this seat**; recorded here against the file so it is findable when that stream next opens the producer. One character (`{sl:+.3f}`), and the check that catches the class is **rendering with a negative fixture**, never reading the format string. **The inverse of [[I-53]]'s family:** I-53 is a check that cannot fire; this is a formatter that cannot fail *on the data it has met*. Both are indistinguishable from correct until the input moves. Found incidentally, 6 Aug 2026, while confirming which artefacts named in a mis-routed ruling existed in this worktree. |
| **I-58** | Provenance / number identity | **DEFECT — instance of the numeral-collision class** | **FOUR REGISTERED NUMBERS DESCRIBE ONE PADDOCK'S TREND, AND A CHECK WAS ABOUT TO COMPARE TWO OF THEM AS ONE.** An instruction specified cross-checking Bala 29ca's report caption of **+0.919** against the same paddock's trend in the methods document, on the stated basis that both cite the same registry row. They do not. **+0.919 is `t10_gap_annual_slope_C_29ca` — an annual reference-*grazed-gap* slope at `zone` support** — while that paddock's *floor* trends are separately pinned as **0.682 raw** (`t10_bala29ca_raw_floor_trend`) and **0.556 water-adjusted** (`t10_bala29ca_water_adjusted_floor_trend`), both at `paddock` support. **A gap slope and a floor slope are different quantities at different support, and only the `number_id` separates them.** | **No — the check was stopped before it ran.** Had it run it would have reported agreement or disagreement between two things that are not the same measurement | **Logged from the design seat, and that is the entry's point: the misidentification happened INSIDE an instruction written to prevent this very class** — which is why it is recorded as an instance rather than a one-off. **Cross-reference [[I-56]]:** resolution by value is not attribution; this is its human form, resolution by *label*, and the remedy is identical — cite the `number_id`, never the value or the phrase. It is also the standing case for CLAUDE.md's **number rule 1** (*a spec cites a `number_id`, not a value*), which this instruction did not follow. Corrected on receipt, 6 Aug 2026. |
| **I-59** | Provenance / number identity | **DEFECT — a collision created by the analysis itself** | **TWO PARTREG STAGE 1 FITS AGREE TO FOUR DECIMALS BY COINCIDENCE.** `2.4_median_weighted` and `2.5_p50` both return a slope of **+0.345**, and they are different quantities: 2.4 regresses the across-year **median** of the p05 series on median inundation; 2.5_p50 regresses the across-year **mean of the within-year p50** on mean inundation. Their intercepts differ by **7.93 pp** (63.41 against 71.35) on y-variables whose means are 66.1 and 78.0, which is the proof the agreement is accidental. **Unlike [[I-58]], this collision was not inherited — the analysis manufactured it**, and it sits in a single CSV where a reader scanning a slope column meets both. | **No.** Both values are correct and both are registered by `fit_id` | **Remedy applied at source, not as a gloss:** the producer emits a `collision_note` column on exactly those two rows, so the warning travels with the data rather than living in a report. Re-ran and re-registered; the table checksums converged. **Cross-reference [[I-56]] and [[I-58]] — the same family from a third direction.** I-56 is a checker that cannot tell citation from coincidence; I-58 is a human reading a value as a label; **this is a producer generating two indistinguishable values from one run.** The common defence is CLAUDE.md's number rule 1: cite the identifier, never the value. |
| **I-60** | Repository / tooling | **DEFECT — method, and it fails silently** | **A `.gitignore` RE-INCLUDE PLACED ABOVE THE DIRECTORY-EXCLUSION THAT GOVERNS IT IS INERT, AND NOTHING SAYS SO.** Ruling CU required one file, `Output/rasters/DATA_share_20260808/README.md`, to become version-controlled. A correct five-line re-include chain was written in the shape of the existing `Output/pack/` and `Output/diag/` blocks and **did nothing**: a *later* line, `Output/rasters/`, excludes the DIRECTORY, later rules win, and git cannot re-include a file inside an excluded directory. The file stayed ignored. The block only worked once moved BELOW that line. **The pattern is positional, so a chain can be correct in isolation and inert in place**, and re-reading the `.gitignore` cannot distinguish the two — the rules looked right both times. | **No — no number, and nothing was lost.** It would have gone the other way: a client-facing document silently NOT tracked while the commit reported success, so the BR and BT corrections would have landed as an untracked file appearing to have always been right | **`git check-ignore -v <path>` is the live query and it names the winning rule and its line number.** Verify every un-ignore against it, never by reading the file — and re-verify that nothing else leaked in, which here meant confirming a dry-run `git add Output/rasters` stages exactly one path. **Applied to the later TEMPORAL-1 block in the same session, which was placed at the end of the Output rules for this reason.** Cross-reference [[I-42]] — same class: a rule that cannot take effect is indistinguishable from one that has, and only a query that reports *which rule won* tells them apart. Found 8 Aug 2026 under Ruling CU; recorded as a named pattern under Ruling CV. |
| **I-46** | Build / determinism | **DEFECT — caught at FIG1-T1, fixed** · **CANDIDATE ROW for pack item T3's NEXT revision** (Ruling V — *not* added to the page now: reopening client prose for a hash-ordering defect is not proportionate, and Part 2 already carries this shape through I-44) | **A PROVENANCE ARTEFACT THAT CHANGES WITHOUT ITS INPUTS CHANGING.** `PACK1_final_number_check.csv` emitted its `NAME_NOT_QUANTITY` rows from `for nm in set(names)`. Python randomises string hashing per process, so **three builds from byte-identical inputs produced three different files** — four lines reordered, no content change. Surfaced only because FIG1-T1 was expected to touch two files and `git status` showed three. | **No number changes** — but it makes every checksum comparison on that file unreliable, and a diff that shows spurious movement is one a reader stops reading | **Fixed: `sorted(set(names))`.** Verified by three consecutive builds hashing identically (`bedc3655…` ×3). **Standing rule (Ruling V), now in CLAUDE.md and enforced as a lint: ANY ARTEFACT WHOSE CHECKSUM IS COMPARED MUST BE EMITTED IN A DETERMINISTIC ORDER** — sets, dicts and anything hash-ordered get `sorted()` before emission. Added to `lint_guardrails.py` as a fourth lint, `hash_order`, proven to fire on a broken fixture. It runs **ADVISORY**, not enforcing: 97 sites exist repo-wide and baselining them would take the baseline from 15 to 112, turning it into the suppression file the `BASELINE_LOCK` exists to prevent. Only loops feeding a checksummed artefact actually matter and a regex cannot tell which; triage the 97 by hand, then enforce. Related to I-44: both are build outputs that were correct in content and wrong as records. |
| **I-45** | Tests / standing rule | **DEFECT — caught at the P4 read-through, fixed** | **A CHECK THAT VERIFIES THE CITATION LIST INSTEAD OF THE SENTENCE CANNOT SEE AN UNCITED NUMBER.** P4-7 was written to resolve every `number_id` in each cell's provenance column, and it passed **38 of 38**. Three cells quoted a number whose id was not listed — Q3's *"about 17 percentage points"* (`t10_bala29ca_xsec_residual`), Q2's four flood ranks with one listed, Q6's *"five survive dropping the two wettest years"* (`t13_recovering_survive_drop2wettest`) — and the check was structurally blind to all three, because **an unlisted number is not in the list it was checking.** Same shape as I-40's seventh instance: the criterion and the thing checked were wrong in the same direction. | **Yes** — it is the pack's final number check, and it certified a state in which a written number had no stated source | **Rule changed (RT-1): extract every number FROM THE CELL TEXT AS WRITTEN and resolve each one.** 38 → **153** numbers checked, 0 unresolved. Two further defects surfaced only because the rule changed: (1) the **first fixture did not fire** — reverting Q3's provenance still passed, because a project-wide declared-source table silently absorbed the uncited 17; a `DECLARED_UNCITED` state now rejects any **result** a provenance-bearing cell quotes without citing, and the fixture then fires correctly. (2) Splitting **PARAMS from RESULTS** was required to make that bite — CLAUDE.md's own three-classes table already says a parameter is the spec and a motivating result is pollution; the check now encodes that distinction instead of treating every numeral alike. **Standing rule: a provenance check must read the prose, not the citation list — and its fixture must be a number the check is obliged to reject, not a citation removed.** |
| **I-44** | Build / self-reference | **DEFECT — caught at P4, fixed by convergence** | **A sheet that reports registry counts, and is itself registered, reports a count that its own registration changes.** `How_we_know` is built from live queries (`table_asset`, `report_asset`), then the workbook is registered — which increments both. First pass wrote **`registered tables` = 4** while the live value became **5** the moment the same script committed. **A stale number on the one sheet that must not carry one**, produced by a generator that was querying live exactly as ruled. Live query is necessary but not sufficient: **the query must run after every write the same build performs.** | **Yes** — it is the pack's provenance sheet, and the stale value was self-inflicted | **Fixed by running the build to convergence:** pass 1 registers, pass 2 re-queries and re-registers via `INSERT OR REPLACE`, and the counts stop moving. Verified: sheet and live agree on 101 / 297 / 5, and a third pass changes nothing. **Standing rule: a generator that both reads and writes a registry must be run to a fixed point, and the fixed point must be demonstrated, not assumed.** |
| **I-42** | Tests / standing rule | **STANDING RULE** · candidate row for **pack item T3** | **A CHECK THAT ERRORS IS NOT A CHECK THAT CATCHES.** A fixture that makes the recompute crash proves only that the code path is reachable; **drift detection requires a fixture that returns a WRONG VALUE the check must reject.** Worked case, 3 Aug: the first page-3 canary fixture renamed `share_a` inside `v_zone_community_composition`, and the recompute raised `sqlite3.OperationalError: no such column: share_a`. The build failed — so on a naive reading the check 'fired' — but nothing had been *detected*; the query simply could not run. Discarded as invalid and replaced with a data-level drift (doubling Bala 29ca Inland's census pixels), which moved the contract SQL from 34.59 to 51.40 and was correctly rejected by the canary. | **Yes, indirectly** — a crash-fixture leaves a check certified as 'proven to fail' when it has never rejected a wrong number, which is how an undetected drift reaches a deliverable | **Recorded in CLAUDE.md beside 'every check must be able to fail', and here.** This is what makes the three RPT-SCOPE canaries trustworthy rather than merely wired. Same family as I-40 — the record was there, the act was not. **Belongs beside the four proved-able-to-fail checks on the T3 sheet.** |
| **I-41** | Registries / standing rule | **STANDING RULE — refines spec §7** | **"Never modify a registered row" was too coarse and was being eroded case by case.** Ruling F, 3 Aug 2026, splits the columns of `dim_headline_number` by field class. **AMENDABLE in place** under explicit design-seat direction, logged each time: `caveat`, `decision_note`, `label` — metadata *about* the number. **NEVER amendable; a change is a NEW ROW and a supersession:** `pinned_value`, `spread_min`, `spread_max`, `support_level`, `scope_filter`, `pixel_constant`, `denominator`, `period_label`, `source_object`, `number_id` — the number and its five qualifiers. Prompted by AD-C (an L-01 warning added to a canary's `caveat`), which read as a second exception to an absolute rule and is in fact a different class of act. | **No** — the rule protects deliverable numbers and the amendable fields carry none | **Recorded in CLAUDE.md standing conventions and here.** Consequence: **AD-C is not an exception**, and the **31 July `floor_flood_*` precision correction remains the single genuine one-off** against the never-amendable list — it changed `pinned_value` and `spread_*`, which is exactly the class that now requires supersession. Two in-place amendments have since been made under this rule and both are logged: AD-C's caveat, and Ruling G's `decision_note` on `t10_refset_inland_share_bala29ca`. |
| **I-43** | Provenance / attribution | **STANDING RULE — I-37 family** | **A RULING IS ONLY A RULING IF IT CAN BE QUOTED FROM A DESIGN-SEAT MESSAGE. If it cannot be quoted, it is a proposal and it stops at a STOP.** Second occurrence of one shape, and this one reached a client deliverable. (1) `AUD1_manifest_delta_REM1.csv`'s `reason_detail` asserted *"design seat 2 Aug — the .png is pack item T1"*; **no such ruling was issued** (recorded at R1). (2) `docs/change_reports/PACK1_P4_assembly.md` cites design-seat rulings **"3 August §2 §3 §5"** which this seat did not issue — and the item list was then overwritten into exactly the state the unattributed `reason_detail` had asserted: M4b deleted, T1 repointed from the `.csv` to the `.png`, `T1_render` deleted. **An unattributed ruling propagated into the assembled pack folder.** | **Yes** — the pack was assembled from the reverted list and shipped 14 files instead of 16, missing M4b and T1's data | **Restored from `6aa345b` and re-assembled under Ruling L (18 rows, 17 items, 16 files).** Both design-seat rulings restated with reasons so they are not reversed a third time: T1 ships **both** — the `.png` delivers four rows side by side in a meeting, the `.csv` keeps the numbers checkable, and there is no trade-off to make; M4b stays **in** because the recovering count reads 8 at the pin, 5 under drop-two-wettest and 5 under a within-community expectation, with only three parts surviving all three, so a reader must see the cut varying on its own.  **THIRD OCCURRENCE, 3 Aug — source: the DESIGN SEAT, and on the page that documents this very rule.** The P4 read-through's RT-3 directed *"APPLY RULING P — the I-44 paragraph into T3's Part 2 … **Text as sent**"*. **No text had been sent.** Ruling P appeared in no design-seat message in the session; the instruction asserted a ruling by reference with nothing quotable behind it, and the artefact it directed an edit to is `Gayini_what_we_dont_know.md`, whose Part 2 states the rule. **CC refused to draft the paragraph and reported RT-3 blocked** — the correct action, and the first time this failure was stopped before it reached an artefact rather than after. The design seat then sent the actual text (Ruling P, P1: *"That is I-43 from the design seat … You were right to refuse; composing it yourself is precisely what the rule forbids"*) and the paragraph was inserted verbatim. **Two of the three occurrences are now the design seat's. The rule binds upward, and this is the instance that proves the refusal path works.** |
| **I-40** | Process / standing rule | **STANDING RULE — I-17 family** · candidate row for **pack item T3** | **RECORDING A DECISION AND EXECUTING IT ARE SEPARATE ACTS, AND THIS PROJECT KEEPS CHECKING ONLY THE FIRST.** Third instance of one pattern: (1) **PIN 1 was correctly recorded as retired and two figure scripts kept drawing it for four days** — the registry said one thing, the code did another; (2) **33 of 43 stale floor-claim sites sat in `docs/` while `Output/` carried the correct, definition-complete statement** — the computation was right and its description was not; (3) **R2, 3 Aug: seven independent derivations were written into `decision_note` and never wired into `test_T8_headline_reproduction.py`**, so coverage read 75% instead of 82.1% and every one of the ten new pins counted as `NO_DERIVATION_PATH`. In all three the *record* was correct and the *act* was absent, and in all three a reader of the record would have concluded the work was done. | **Yes, repeatedly** — a documented-but-unexecuted decision is indistinguishable from a done one at reading distance, which is exactly how a wrong number reaches a deliverable | **STANDING RULE: a gate that records a decision must also name the artefact that executes it, and the acceptance check must interrogate the ARTEFACT, not the record.** Concretely: a retired number is not retired until a repo-wide scan shows no live code querying it (already spec §6); a corrected claim is not corrected until the `Output/` artefact it cites is re-read; **a derivation is not a derivation until the test recomputes it and the coverage number moves.** The R2 self-catch is the model — the discrepancy surfaced only because the coverage figure was recomputed from the test rather than asserted from the report. **Marked as a candidate row for pack item T3.** **SIXTH INSTANCE, 3 Aug — and it was issued from the DESIGN SEAT, not from CC.** Ruling L4 directed *"KEEP ALL OF P4's MACHINERY … Re-run, do not rewrite"* — but **no producer existed**. The pack folder and `PACK1_assembly_manifest.csv` were on disk with no code behind them; the original was ad-hoc and never saved. The instruction rested on a fact inferred from the manifest's column quality and asserted without verification. **This is I-40 inverted, and the rule binds upward or it does not bind.** Recorded at the design seat's own direction (Ruling M1). **SEVENTH INSTANCE, 3 Aug — a new shape: AN ACCEPTANCE CRITERION THAT PASSED BECAUSE THE CHECK AND THE THING CHECKED WERE WRONG IDENTICALLY.** P1's criterion read *"15 distinct non-null paths"* and the build reported 15, so it passed — but `T1_render` had been ruled a shipped file at P1-4 and was never added back to the count, so the deliverable was **16** all along. **A criterion supplied by the design seat and a count produced by the build are NOT independent when the criterion is typed rather than derived** — both were carried forward from the same stale arithmetic. Caught only when the Ruling L re-assembly produced 16 against a criterion of 15. **STANDING RULE for the T3 sheet: an acceptance criterion stated as a literal is not a check; where a count is derivable, the criterion must derive it.** **EIGHTH INSTANCE, 3 Aug — source: the DESIGN SEAT.** A **13.33% woody cover** figure was carried into the pack item T3 draft as a scientific limitation. **It does not reproduce.** `census_by_zone_stratum` gives Floodplain Woodland / Forest at **86,375 of 1,080,157 px = 8.00%**, Other / minor units **4,951 = 0.46%**, and the non-treed analysis scope **988,831 = 91.55%**, summing exactly to the census total. **Nothing in the database yields 13.33%.** The figure entered via the R6 LiDAR review — a TaskU product at `qa_status = REVIEW`, **unshippable by its own registration** — and reached the draft **from a review document without a database check**. Withdrawn and replaced with the statement the analysis supports: the floor excludes the treed stratum entirely and is a ground-layer measurement over nine of eleven strata. **The pattern again: a number was recorded, and its verification was not.**  **NINTH INSTANCE, 3-4 Aug — source: the DESIGN SEAT.** `Gayini_R6_metric_review_20260802.md` established that R6's residuals are computed on the census temporal p05 while the deficit they are compared against is computed on `veg_p05_spatial` — *"the prohibited comparison, and the sole basis for 'the anomaly has dissolved'"* — and gave the re-run: Bala 29ca **−29.04** and **−18.67** against R6's **+1.57** and **+9.61**, with the Aeolian community fit reversing sign. **It was written and never propagated.** Both reference-state stream documents went on carrying *"The reference-state anomaly has dissolved"* and *"R6 closes it"* as their headline conclusion, with zero mentions of `veg_p05_spatial`, until LID-1 Gate L1 found them. **Remediated 4 Aug (Ruling W): a superseding block at the top of both, verbatim, with the sections left as written — a retracted conclusion that is still readable is worth more than one quietly reworded.** **TENTH INSTANCE, 4 Aug (LID-1, Ruling AA): ten `dim_headline_number` rows were RULED AND NOT INSERTED.** Task U summary §9 records *"2 denominators registered; 10 further rows ruled and pending insertion at closeout C1"*; the live registry holds exactly the 2. **Deliberately NOT inserted** — Task U is closed and six days out, and inserting ten rows into a closed task's registry is the tidying that breaks something. **Consequence, stated in the LID-1 L2 output: every Task U quantity except the two denominators is unpinned and outside `test_T8_headline_reproduction.py`.** |
| **I-39** | Registries / rules | IMPROVE — **I-17 family** | **A selection rule that picks numbers to pin had no "and does not already have a `number_id`" clause.** R2's pin rule (*pin a SOURCED number if quoted in more than one deliverable, or if it is a numbered register §1 claim*) selected 10 candidates; **3 were already pinned** — `t10_gap_annual_slope_C_29ca`, `t13_parts_declining_count`, `t10_bala29ca_xsec_residual`. Registering them again would have created a second row for one quantity, which is discrepancy class #1, arrived at by a rule intended to *improve* provenance. The rule also tripped its own >8 stop threshold purely on the duplicates. | **No** — caught before the write; nothing registered | **Clause added and applied at R2: a pin rule must exclude quantities that already carry a `number_id`.** With it the rule selects 6 directly and does not trip the threshold. Generalises: **any rule that adds rows to a registry needs an existence test before a merit test**, otherwise the merit test is doing duplicate-detection it was not designed for. Sibling of I-17 (multiple dated copies of one artefact) at the row level rather than the file level. |
| **I-37** | Provenance / numerals | IMPROVE | **The numeral 18 names three different objects in the reportable-plot / area space, and they are easy to swap.** (a) **18 unzoned reportable plots** — a *count*, = **31.6%** of the 57 reportable plots. (b) **18 zoned 14-day-grazing reportable plots** — a different count that happens to equal the first. (c) **18,561.5 ha** — `property_outside_mapped_ha`, an *area*. Caught at R1b when this session wrote "a fifth of the reportable plot set" (20%) for what is 31.6%, and the design seat read the error as a confusion with "the 18% unzoned share by area" — which is itself **35.7%** (`total_no_management_zone_ha` 30,711.6 / `TRUE_FARM_HA` 85,910.8), not 18%. So three readers reached for the same numeral and none of the three readings matched. | **No** — no deliverable number is wrong; the plot counts and the area pins are individually correct and registered | **Report only.** Rule: **whenever "18" appears near plots or unzoned country, state the unit and the denominator in the same sentence** — "18 of 57 reportable plots (31.6%)" or "18,561.5 ha outside the mapped census". The share-by-area figure (35.7%) is derivable from two pinned rows but is **not itself pinned**; pin it if it ever reaches a deliverable. Neighbour of C-07 (composite support_level) and the D8 two-floors pair — one appearance, several meanings. **FOURTH OCCURRENCE, 3 Aug — and the worst, because it is now a persisted DB field.** T3 Gate D wrote `dim_management_zone.reference_set_caveat`, which reads: *"three of the four carry RAP plots and Bala 27ca has none, so a plot-network count gives 3 and a polygon count gives 4."* That is correct, and it describes a **different three from ours**. **THREE "threes" are now live:** (i) the **plot-network three** — 26ca, 28ca, 29ca, excluding 27ca for having no plots (the DB caveat); (ii) the **reference three** — 26ca, 27ca, 28ca, excluding **29ca** as the outlier (ours, and the basis of register claim 1); (iii) the polygon **four**, `reference_set_member = 1`. **(i) and (ii) share only 26ca and 28ca.** A future reader reaches the DB caveat before they reach our register, and the two threes are not distinguishable from the numeral. **Mitigation applied at R2:** the pin is `ref_grazed_gap_annual_ref3_excl29ca_mean` — the exclusion is in the `number_id` itself — and its `scope_filter` names all three paddocks and states explicitly that it is NOT the plot-network three. **Standing rule: "three paddocks" unqualified is now forbidden in this project.** **FIFTH OCCURRENCE, 3 Aug — "6 of 9" names two different measurements.** F6's caption carries *(above in 6 of 9 strata; plot-confirmed above in 8 of 9)* from `v_three_arm_gap_decomposition` on **`veg_p05_spatial`** (pins `three_arm_unzoned_inferred_above_14day_count` = 6 and `..._plot_...` = 8). By_question Q2 carries a different *6 of 9* from `v_zone_stratum_contrast_bala_robust` on **`veg_p05_mean`**. CC conflated them and briefly reported that a pack figure rested on the temporal p05; the producer references that view **zero times**. **The matched pair (6, 8) is what distinguishes F6's — a lone "6 of 9" is ambiguous in this project.** **SIXTH OCCURRENCE, 3 Aug — "three parts, two of them in Bala 29ca" names two different results, and this one was caught one gate before the pack's opening page.** (a) **T13 sweep core** — parts Recovering at *every* swept cut 0.50–1.50: **Bala 15 Inland, Bala 29ca Aeolian, Bala 29ca Riverine** (verified from `T13_gateC_classification.csv`; `level_z` −3.986 / −2.695 / −2.293). (b) **T14 metric sensitivity** — the three surviving T13's cut, drop-two-wettest *and* a within-community water expectation; **membership unverifiable here, R3 is cut and it has no committed producer**. Both are "three parts, two in Bala 29ca", from different tests. A draft of Start_here claim 6 asserted (b); it now ships (a), which is fully pinned. | **Yes — it was one gate from the pack's opening page** | **Claim 6 rewritten to the sweep core (Ruling O2), which is pinned and verified. The (b) contrast moves to pack item T3 as a limitation, marked a design-seat computation with no committed producer (Ruling O4).** Record both memberships side by side so nobody merges them later: (a) is a *cut-stability* result, (b) is a *metric-definition* result, and they answer different questions. |
| **I-36** | Tests / reporting | **DEFECT — the summary string, not the test** | **`test_T8_headline_reproduction.py`'s exit line mixes two denominators and is genuinely ambiguous.** It prints `"{len(fails)} DRIFTED of {checked} checked"` — but `checked` counts **only** rows that have a recompute path, while `fails` counts **both** value drifts **and** rows with no path. The missing-path rows are therefore *not a subset of* the checked rows. Live: `"14 DRIFTED of 71 checked"`, which reads naturally as *57 of 71 reproduce (67%)* but actually means *71 of 85 pinned rows reproduce (84%), 14 have no derivation, 0 drift*. `14 + 71 = 85` = the pinned rows, which is the arithmetic that settles it. Two readers took the two different readings; register v3 recorded "57 of 71", a figure that appears nowhere in the data. | **Yes** — the coverage fraction goes on the pack's `How_we_know` sheet under a client cover, and the two readings are 67% vs 84% | **Diagnosed, not yet fixed.** Settled for now by `scripts/11_database/build_RPTSCOPE_reproduction_status.py`, which emits a per-row status table (`Output/tables/RPTSCOPE_reproduction_status.csv`, 88 rows: 71 REPRODUCES · 14 NO_DERIVATION_PATH · 3 NOT_PINNED_deliberate) and prints the four counts explicitly. **Any coverage number must come from that table, not from the summary string.** The fix in the test itself is a one-line change — separate `no_path` from `drift` and print all three counts — but it edits a green test during a delivery window, so it is post-deadline. **Rule meanwhile: never quote the exit string; quote the status table.** |
| **I-32** | Figures / acceptance | **DEFECT — class, not instance** | **Acceptance checks in this project test data objects, never rendered output, so a figure can pass every check while being wrong.** Worked case (Adrian pack T1, 31 Jul): `fmt()` used **`ifelse()`**, which returns a value the length of its **TEST** — the test (`sign`) was length 1, so the formatted number collapsed to a single element and **`sprintf()` recycled it across all four paddocks**. The figure rendered `45.3` in every column. The CSV was correct, and all five value-level acceptance checks passed, because they test the data frame and not the ink. **The PNG is what the client sees.** Generalises beyond `ifelse`: any length-1/length-n mismatch feeding `sprintf`/`paste` recycles silently, and R will not warn when the longer length is a multiple of the shorter. | **Yes, in the worst way** — a wrong number reaches a client deliverable while the registry, the CSV and the test suite all read clean | **Fixed for the class, not the instance.** New `R/gayini_assert_rendered.R`: `gayini_assert_rendered_values()` (each drawn string contains its source value), `..._varies()` (uniform strings where sources differ = recycling bug), `..._caption_number()` (a number quoted in prose matches its source at the precision the prose states), `..._table()`. **Standing rule: every figure that renders numbers as text asserts the rendered STRINGS against the source values.** Applied to the T1 table (values + ranks), F5 callouts, and the T13 Gate D community-SD caption line. **Guard proven to fire**: reintroducing the original `ifelse` in a fixture halts the build with `[T1 flood %] 3 of 4 rendered strings do not contain their source value`. Extend to any new numeric-label figure. **Two further members of the class, both found at T11 v2 (31 Jul), neither visible to any value check:** (a) **`theme_void()` + `ggsave` writes a TRANSPARENT PNG** — no `plot.background` is supplied, so the file renders as **black** in most viewers and takes black title text and legend labels with it. The Gate B residual map was unreadable and its title invisible while every number in it was correct. Fix: set `plot.background = element_rect(fill = "white", colour = NA)` on any `theme_void()` figure. (b) **A caption asserting a fill the figure does not contain** — the dual-grain subtitle promised "pale fill = not assessed" while the base layer was the union of the parts, so no pale ground could ever show. The inverse of the T13 white-fill defect: there, one appearance meant two things; here, a stated meaning had no appearance. **Both reach a client through a route no data-level check can see, which is the whole point of this issue.** |
| **I-31** | Registries / DB | IMPROVE | **No registry exists for a derived VECTOR build output.** `raster_asset` covers rasters, `census_asset` covers the parquet, and `spatial_layer_asset` is an **import** registry — it records layers read in from `Input/`, so a build-output row there is a category error (the same one CLAUDE.md already names for the `Gayini_Results.gpkg:management_zones` companion). Surfaced at T13 Gate E: `Output/spatial_8058/T13_part_polygons_epsg8058.gpkg` is the exact geometry behind a **registered** classification (`fact_zone_community_part_classification`, 115 parts) and its render-only twin, and neither can be registered anywhere without committing that category error. Both were therefore deliberately left unregistered. | **No** — no number changes. But the geometry behind a registered number currently has **no in-DB provenance row**: the chain is complete only via the exit bundle and the two tracked producing scripts | **Do not improvise a home.** Registering build outputs in `spatial_layer_asset` would corrupt an import registry and break `read_registered_layer()`'s contract. The fix is a new `vector_output_asset` table on the `raster_asset` pattern (path, layer, CRS, feature count, first-50-MB SHA-256, `path_exists`, `run_id`, producing script). **Post-deadline.** Meanwhile provenance lives in `Output/review_bundles/t13_paddock_part_classification.zip` and `build_T13_gateD_part_polygons.R` / `build_T13_gateD_render_geom.R` |
| **I-02** | Spine / T2 | **BLOCK** | Within-stratum `veg_p05` spread across the reference paddocks (fids 1–4) **exceeds the reference-vs-grazed contrast in 6 of 9 strata** — every Riverine and every Inland Floodplain band. Riverine-high: 36.5 pp within-reference (29.83→66.33) vs +8.25 pp pixel-weighted contrast (zone-support even smaller, +3.6/+0.1/−2.1). **A fixed distance-to-reference target is undefined in those strata.** Quantified at T2 Gate A (`v_zone_stratum_treatment_contrast` × per-fid `v_census_by_zone_stratum`). | **Yes** — T2 is the substrate for that metric | **Spine decision, not a build one.** The substrate is still correct to build (T2 proceeds). Then a spine-chat decision: narrow reference / per-stratum target / environmentally-defined (HCAS 3.3) / report heterogeneity as the finding. Do not work around it in the build |
| **I-03** | Task J | **EXTERNAL** | Jana email unsent — cut-date provenance (L07) and bank geometry (L10). Sole remaining blocker on Task J. | No — analysis complete | Send one email and proceed regardless, per `Gayini_path_to_Aug10.md`. Outstanding since ~18 July |
| **I-04** | T2 / S5 | **EXTERNAL** | `dim_management_zone.cropping_history` and four other RESERVED columns NULL pending Ernest's land-use table. So conserved-vs-**grazed** is available; conserved-vs-**formerly-cropped** is not. | Constrains the claim, not a number | Build what the data supports; name the gap in every caption. Graduate to limitations register |
| **I-05** | T3 | **EXTERNAL** | Adrian's LiDAR shrub-height model has not arrived. | No | T3 Gate E produces the overlay package so the overlay is a five-minute job on arrival |
| **I-06** | Spine §5 | DEFECT | S2 labels the plot-support 9 / 22 / 50 / 44 gradient "Support: stratum, pixel". The numbers are right; the label is wrong. `dim_metric.support` already stores both rules verbatim. | No — numbers correct | Spine-chat text fix. Also flagged for T4 Gate A |
| **I-07** | Spine §5 | DEFECT | C-3 states census `veg_p05` range as `[1.19, 97.00]`. 97.00 is the **`veg_p50`** max; p05 is `[1.19, 91.85]` all-pixel, `[1.19, 88.66]` non-treed. Conclusion (percent, no offset) stands. | No | Spine-chat text fix |
| **I-08** | T1 Gate D | DEFECT | `min_cell_n` flags on **pixel** count and is actively misleading. It flagged Aeolian-high (2,501 px) and Riverine-high (2,223 px) while **passing Riverine-low at 7,877 px — 93% one paddock.** The flag cleared the cell that produced the false headline. | No — the finding is now correct | The view still carries it and someone will trust it. Add a treatment-unit-count flag alongside, or annotate. Cheap |
| **I-09** | T1 Gate D | DEFECT | Aeolian Bala-only contrast is **1 paddock vs 1 paddock** (+21–24 pp). Must be reported as a paddock description, never as a contrast. | Yes if written up wrong | Wording constraint, recorded. Do not present as a treatment effect |
| **I-10** | Tooling (B5) | DEFECT | `run_spine_smoke_test.R:104-112` (`folder_scripts/archive_absent`) **hard-fails if `scripts/archive/` exists** — inverted against the archive convention it appears to enforce. A check that cannot fail. | No | Unresolved, Adrian's call. **Do not modify the smoke test to force it.** Use `lint_guardrails.py` exit 0 as the acceptance signal instead |
| **I-11** | Tooling | DEFECT | `run_spine_smoke_test.R` exits 1 on `structure/folder_scripts/10_downstream_optional` = missing, plus three `outputs` warnings. Pre-existing. **A permanently-red test gets ignored exactly like a permanently-green one.** | No | Same bucket as I-10. Post-deadline |
| **I-12** | Docs | DEFECT | Shipped data dictionary is behind the DB — `census_stratum` and `census_asset` absent from the `fields` sheet; `raster_assets` sheet has no `product` column. Recurrence of correction C5. | No | Regenerate from live DB. Post-deadline unless T4 runs |
| **I-13** | DB | DEFECT | `dim_metric.support` NULL on **36 of 45 rows** (populated for 7 pixel + 2 plot metrics). C10 leans on this column. | No | Treat unpopulated as unknown, never as either. Backfill where derivable — T5 Gate 4 |
| **I-14** | DB | DEFECT | `figure_asset` pre-Gate-E rows are an old-generation snapshot, unreconciled against disk. The 11 Gate E + 4 T1 + 2 Gate C rows are current. | No | Do not backfill. Reconcile post-deadline |
| **I-15** | Repo | DEFECT | `scripts/_deprecated/` violates the `scripts/archive/` convention — but cannot be reconciled without tripping I-10. | No | Blocked on I-10. Leave untouched |
| **I-16** | Output folder | DEFECT | 16 migration blockers remain (8 hard via `builder_csv_input`, 8 soft via `path_module_resolved` only). Two shadow figure registries feed nothing. | No | Explicitly parked before Aug 10 per `Gayini_path_to_Aug10.md` |
| **I-17** | Repo hygiene | DEFECT | Three dated contract snapshots in `docs/` (25, 26, 27 July). **Multiple dated copies of one artefact is discrepancy class #1** and has already misled four readers. | Yes, indirectly | Keep only the newest. Fold into the snapshot generator: delete predecessors on write |
| **I-18** | Process | IMPROVE | `T1_A_identity_margin.png` upper panel runs 0–0.15, compressing the band it exists to show into a thin strip. | No | Post-deadline, if the figure is ever regenerated |
| **I-19** | T5 | IMPROVE | Gates 2–4 deferred: QA verdicts → live views, `computed_at` / `v_qa_freshness`, `read_registered_layer()`, closed-ladder trigger, mixed-support detector. | No | Post-deadline. Gate 1 is done and has already caught a live violation |
| **I-20** | T4 | IMPROVE | Whole task deferred — `claim_register` and the spine evidence workbook. It is the durability artefact that stops C-1 recurring. | No | Post-deadline. Genuinely valuable; not an Aug 10 deliverable |
| **I-21** | T5 / tooling | IMPROVE | Add a `hardcoded_path` lint alongside `magic_number` / `or_ignore` / `whole_digest`. T2's Gate B/C scripts shipped session-scoped temp paths (UUID dir) that the numeric-only lint could not see; caught in review, not by the guard. A path lint (flag absolute paths / temp dirs / UUIDs in `scripts/` and `R/`) would have. | No | Post-deadline. Log now; do not build during T2 |
| **I-25** | T2 | IMPROVE | T2's per-**community** extraction (`fact_zone_community_veg_annual`) pools wetness bands, so the reference-vs-grazed gap decomposition carries a latent wetness confound. T6 showed the fix: extract by **stratum** (community × regime_band) controls wetness by construction (residual ≤3.3 pp vs a 26 pp gross skew). T2's two-arm decomposition was never re-cut at stratum grain. | Not for the deck | Post-deadline. Re-cut T2's gap decomposition at community × regime_band if the reference-vs-grazed floor number is ever used quantitatively. Not T6's job to fix; logged per Hugh's instruction |
| **I-24** | Process | IMPROVE | CLAUDE.md carries conventions that no acceptance gate checks (the review-bundle one was invisible until built — see C-12). Others may be equally unenforced (e.g. "one figure = one file = one slide", "every caption states support level", the review-bundle-per-task copy step). | No | Post-deadline. Audit CLAUDE.md conventions against the acceptance checklists / smoke test / lints; enforce or retire each |
| **I-26** | T12 / Task J | DEFECT | `Input/gayini_vectors_8058.gpkg` carries `irrigation_bank_cuts` (1,158 features) — a **Task J** input — that is registered in no `spatial_layer_asset` row. Found during T12 Gate A0 while registering the 8058 boundary/plots/community layers from the same GeoPackage. | No — outside T12's scope | **Log, do not fix** (per T12 spec Gate A0). Register under Task J when that trail is next touched, using the T1/T12 Gate A0 pattern. Not a T12 deliverable |
| **I-27** | Docs | DEFECT | CLAUDE.md "Current state" understates two counts: `spatial_layer_asset` **6→9** (T12 Gate A0, `spatial_007/008/009`) and `raster_asset` **126→166** (already-stale 126; +38 T12 Gate A). Stale prose vs data — the exact discrepancy class the provenance rules target. | No | **DONE at T12 close (28 Jul) → C-16.** Single CLAUDE.md edit applied: `spatial_layer_asset` 6→9, `raster_asset` 126→166, `figure_asset` 255→278, the T12 `dea_` objects, and the standing git rule (retires the branch/PR flow, I-28). |
| **I-28** | Process / git | — | **Standing git rule adopted (28 Jul 2026).** CC now: runs `git fetch/status/log` at session start and reports branch tracking + whether main moved; **pushes every gate** (`git push -u origin <branch>`, no approval, never batched/deferred, report the SHA); and **merges approved gates into main once per day** at end of day (fast-forward where possible), only for gates approved in chat. Never force-push, never rebase pushed work, never commit rasters/SQLite/large spatial data, no AI attribution — commits authored solely by Hugh. | No | **Deliberate relaxation of the human-merge convention.** Reason: the in-chat gate review is the substantive gate; the GitHub merge was ceremony. Backup (push) is decoupled from publication (merge). CLAUDE.md wording folded into the I-27 single close-out edit |
| **I-30** | Tooling / lint | DEFECT | **The magic-number lint blocks two spellings, not the class.** `MAGIC_LITERALS` bans the exact strings `0.0625` and `0.062351428`, so a *rounded-correct* bare literal for the 8058 pixel area slips the lint while still bypassing `gayini_params`. Three tracked scripts do exactly this: `scripts/05_ground_cover/03_h2_seasonal_gate_and_diagnostics.R:371` (`0.0623514`), `scripts/05_ground_cover/06_taskM_gateD_p05_ge80_contiguity.R:24` and `scripts/11_database/taskM_gateD_p05_distribution.py:53` (`0.0623512`). Values are correct-to-rounding (~0.0004% off, NOT the 0.238% `0.0625` error) and feed Task M diagnostics, **not** the 66 site / 21 paddock reports. Found in the T8 `0.0625` audit (`docs/change_reports/T8_pixel_area_constant_audit.md`). | No — no material area error; canonical areas route through `gayini_params` / `terra::res` | **Report only, not fixed** (per instruction — a fix in any report-adjacent path is routed via the report stream). Harden the lint to flag any bare decimal in `[0.062,0.063]` (and 30 m `0.09`) outside the param module — a T5 item, neighbour of I-21. |
| **I-29** | T10 / reference-state deck | **BLOCK** | The **five-period split** (1988–92 / 1993–2002 / 2003–12 / 2013–18 / 2019–22) — the deck's central §5 table (Reference ×4 gap −13.1 → −5.6; Bala 29ca −42.3 → −18.0) — is **produced by no script in the repo.** T10 Gate A exhaustive search (28 Jul, all 429 tracked text files): only Task J (2018-centred periods) and T12 (DEA sensor eras) carry ≥3 of the five boundary years; `T2_gateE_figures.R` writes a **two-window** report (early ≤1997 / late ≥2013, ±2 pp rule), not five periods. Independently confirms `Gayini_reference_state_methods.md` §8 ("the five-period split in §5 is not produced by any script"). | **Yes** — it is the deck's central table, and a figure nobody can reproduce is a number that doesn't reach anything | **Do not rebuild silently** (T10 Gate A instruction). The derivation must be located or rebuilt at **T10 Gate B** — a separate gated step, not this recon session. When rebuilt, reconcile against the design-seat regression (r=0.710, slope +0.548, grazed-median flood 28.6%) per T10 Gate B. |

| **I-33** | QA / render currency | **DEFECT — method, not instance** | **A file-timestamp test for render currency is a screening tool, and it fails in both directions.** AUD-1 flagged seven pack figures as Category G by comparing file `mtime` against an event calendar (the 28 Jul pins, the 31 Jul `floor_flood` precision correction, the 31 Jul QA-2a guard). REM-1 resolved all seven. **Six were false positives** — F1, F2, F4 (draw no affected number) and M5, M5b, F5 (read `floor_flood_*` from `dim_headline_number` at render time, so they were already current; the 62-second gap to the precision correction was harmless). Each was settled by scratch-rendering to a temp path and diffing: all six byte-IDENTICAL. **The seventh, F6, was real but invisible to the test that flagged it.** `T6_gateE_figures.R` queried `fact_three_arm_gap_decomposition WHERE regime_band='ALL'` — the rollup PIN 1 retired for drier-skew confound — and that rollup *still returns* the superseded values (−19.65 / −11.70 / +1.07). F6 was never a stale render; it was a **live render of a retired aggregation**, and it would have survived any re-render-based remediation byte-for-byte, looking corrected and being wrong. mtime says nothing about which rows a query selects. | **Yes** — the false-positive half wastes gate time on figures that are fine; the false-negative half is the class that reaches a client | **Screening only; never a verdict.** Two checks actually resolve currency, and neither involves a timestamp: (1) **render-and-diff** — re-render to a scratch path with `write_and_register_figure()` shimmed to a plain `ggsave`, and compare bytes; identical means current, on evidence, with no registry write; (2) **read the query behind every drawn number** — confirm the scope filter matches the pinned `scope_filter`, because a figure can be perfectly current with respect to time and still select a retired aggregation. QA-2b is rebuilt on this basis. The second check also answers whether each report-figure number traces to a nameable source, so the two collapse into one pass. **Successor work, found at REM-1 close:** `test_T8_headline_reproduction.py` reported `2 DRIFTED of 71` before REM-1 and **`14 DRIFTED of 71` after** — the two TaskU rows plus all twelve REM-1 rows, every one `recomputed=NOT RECOMPUTED`. Registration does not confer test coverage; nothing enforces the second step after the first. **Do not close it by copying the registration logic into the test** — CLAUDE.md requires an INDEPENDENT re-derivation, and a copied path would pass by construction and prove nothing, which is precisely the defect shape the test exists to catch. Needs a genuine second derivation per uncovered `number_id`. |
| **I-34** | Registries / DB | IMPROVE | **`table_asset` is not builder-integrated.** REM-1 Gate C created `table_asset` (the registry `Output/tables/` never had) on the existing registry pattern — same `path` / `product` / `run_id` / `path_exists` / `checksum_sha256` / `qa_status` / `superseded_flag` conventions as `raster_asset` and `report_asset` — and registered the two register-v2 pack items in it. **The builder does not create it.** A full builder re-run would destroy the table and both rows, exactly as it already would the 9 EPSG:8058 Task H rasters, `census_asset` and `dim_metric.support`. | **No, while the standing rule holds** — the builder is never re-run, and no non-destructive registration strategy exists yet | **State it, do not fix it here.** `table_asset` joins the list of post-build manual registrations under CLAUDE.md **Database**; add its creation + registration script to that sequence when the non-destructive registration strategy is built. Precedent worth noting: this is the same shape as **I-31**'s proposed `vector_output_asset`, and `table_asset` is now the working example of that pattern — build both together. |
| **I-35** | Provenance / Output | IMPROVE | **`Output/tables/` is an entire output class that was outside provenance.** At AUD-1's Gate A snapshot it held 83 files and **no registry covered any of them**; it now holds 93 (TaskU added tables during the audit) of which **2 are registered** — the two register-v2 pack items, done at REM-1 Gate C. The remaining 91 include the T13 sweep and robustness tables, the T6 in-scope point sets, the taskM diagnostics and every taskU Gate U1/U2 output. This is structural, not a one-off: results tables had nowhere to be registered, so none were. | **Not before 10 August** — the only two tables named by a pack document are now registered, and unregistered tables block nothing else that ships | **Do not bulk-register.** A sweep would stamp provenance on files nobody has checked, which is worse than an honest gap: `run_id` and `product` have to be established per table, not guessed. Post-10-August job, sequenced after **I-34** so the registry is builder-integrated before it is filled. Until then `table_asset` row count is a deliberate 2, not a partial migration. |

## Closed

| ID | Issue | Resolution |
|---|---|---|
| **C-01** | `plot_rs_analysis_base.csv` reported missing, blocking script `05` | **Present** at `Output/csv/canonical/` — gitignored, not missing. Script 05 can run |
| **C-02** | Reference set — three or four conserved paddocks? | **Four.** Fids 1–4 (Bala 26ca/27ca/28ca/29ca), all `zone_group = 'Bala'`, `grazing_excluded = 1`. The "three" was a miscount, confirmed from the DB |
| **C-03** | `raster_asset` extents unpopulated ("98 of 98") | **Stale QA row dated 2026-07-01.** All 126 rows populated. Misled four readers including the spec author, twice |
| **C-04** | `Area_MW` / `ManagmentZ` do not exist | **They do** — in `management_zones_epsg8058.gpkg`, capitalised ESRI names. The spec had inspected the wrong layer. Three management-zone objects exist, not two |
| **C-05** | Zone identity unverifiable (27 of 64) | **All 64 verified.** Provenance + 62/64 assignment closure. `{9,21}` are area-twins held by provenance alone — recorded as `provenance_only` |
| **C-06** | Refugia "~4,300 ha" — three competing lineages | **Withdrawn** by Task M (D8). `green_at_floor()` measures green *share*, not total cover. The grid mismatch explained only the 6,458 ↔ 4,474 pair |
| **C-07** | Composite `support_level` | Split into closed-ladder `support_level` + free-text `aggregation_unit`. **Specced four times before it was caught** — logged as a method trap in lineage §9 |
| **C-08** | The 0.0625 pixel-area error | `gayini_params.PIXEL_AREA_HA` derived, never typed; magic-number lint fails on a bare literal. Caught a stray literal in its own author's script within hours |
| **C-09** | `OR IGNORE` passing a stability test while the DB is wrong | `INSERT OR REPLACE` enforced by lint; idempotence now tested by **convergence** |
| **C-12** | Review-bundle convention was in CLAUDE.md but in no acceptance checklist (was I-23) | **Mechanised.** `scripts/utils/build_review_bundle.py` (spec-driven) now produces `Output/review_bundles/<tier>_<task>/` + zip + MANIFEST (scripts w/ git status, DB objects w/ row counts + CREATE SQL, registry ids + checksums, artefact sizes, deletions, and every acceptance criterion with its number). Run for T2 (`docs/review_bundles/T2_zone_annual_veg_spec.json`). Bundle 1.6 MB; >10 MB artefacts referenced by path+checksum, not copied |
| **C-11** | D2 site dashboards "5 of 66" — 9 unbuilt (was I-22) | **57 of 66 is the deliverable.** The 9 unbuilt are exactly the `ground_cover_exclusion_flag = 1` treed Floodplain Woodland/Forest plots — the context/treed set the project excludes from ground-cover interpretation. **Decision (Hugh, 27 Jul): leave unbuilt** — building them would need a ground-cover-free variant for a set that is deliberately set aside. 57 = the analyzable plots. The composer's `limits length 1` error (`gayini_dashboard_panels.R:54`) stands unfixed by design; do not build the 9 |
| **C-10** | FC values >100 in the annual veg stacks (max 108 mean / 110 jja_son) — unmixing overshoot or water contamination? (was I-01) | **Bilinear-resampling overshoot**, traced to `legend_semantics` ("bilinear" to the 8058 grid). Only **24 over-100 pixel-years of 143.1M valid (0.00002%)** in the primary, 622/133.95M (0.0005%) in jja_son. The 3.5× wet-enrichment is on n=24 — noise; real water contamination would be thousands-to-millions of wet-year pixels. **Keep raw, no mask, no clamp**; add `n_px_over_100`/`pct_px_over_100` counters; tolerance stated from the FC product domain [0,100], not the observed range |
| **C-13** | Spec identifier collision — two artefacts numbered **T7** (the DEA Land Cover extraction, and "T7 — Persistence surface" in `Gayini_reference_state_specs_T7_T11.md`). Class: **duplicate identifier** — discrepancy class #1 territory. Flagged at T12 Gate 0 recon. | **Resolved by renumbering the DEA task T7 → T12** at spec v2. `docs/reference_update/T7_dea_landcover_l3_extraction.md` archived to `docs/archive/` with a superseded-by header (not deleted); `T12_...md` v2 is the live spec. No number was ever produced under the ambiguous identifier |
| **C-14** | T12 §2.4 v1 `dea_indeterminate` rule ("< 5 valid years in the era") made the **2019–2022 era indeterminate by construction** — that era is only four calendar years long, so it could never reach five. A defect in the rule, not the data. | **Corrected at spec v2**: "< 4 valid years **or** < 60% of the era's calendar years present"; floor window 2023–2025 exempt (calibration input, never classified). **Caught before any number existed** — the classification gate (Gate D) has not run |
| **C-16** | T12 close-out (was I-27 doc-sync + Gate B farm/community decision) | **T12 closed as a documented negative** (spec v4 §2.7 fired). CLAUDE.md single edit applied (I-27 done). **Gate B farm/community as separate `dea_`-prefixed tables: confirmed by Hugh, no rework.** 42 zone-era false positives recorded (§2.8: never call them cultivated); `cropping_history` NULL 64/64 is now a finding (DEA cannot fill it), not a gap. Methods slide (sensor-era gap), 2 maps, spine return (S5 gains no land-use variable), limitations rows L-T12-a/b/c staged. Next: 66 site reports |
| **C-15** | Refugia branch `fix-refugia-changelog` — should it merge to main? | **Deleted deliberately (local + remote, 28 Jul), unmerged.** Merging would have **regressed** main: main's `Gayini_established_data_facts.md` quick-facts row 5 already carries the discipline-correct, **number-free** wording deferring to `Output/tables/taskM_green_at_floor_area.csv`; the branch hardcoded "~6,460 ha supersedes ~4,300 ha". Commit `e87bd1a` survives as a git object if ever needed. **Canonical number, read from that CSV this session: `area_ha_native_30m_3577 = 6,457.95 ha`** (majority-green floor = `green_frac_pct = 100·PV/total_veg > 50`, paired at each pixel's total-veg p05 season; native 30 m / EPSG:3577; 959,833 valid px; 7.476%). This ≈ "~6,460 ha" and **does reproduce** from the native `green_at_floor()` route (`scripts/05_ground_cover/04_taskM_green_at_floor_area.R`). **Correction to the chat framing:** the `Tier2_TaskM_deck_evidence_audit_v2` "unverified" flag was specifically about the census-**parquet** route (parquet holds `veg_p05` = total cover, a *different variable* = 40,935.8 ha) and about the number floating in prose without citing its artefact — **not** about the value being unreproducible. So ~6,460 was the right value; it needed to cite this CSV. `~4,300 ha` remains withdrawn (C-06 / D8). Five group-A docs bannered (not rewritten — the audit trail is preserved); groups B/C left alone |

---

## Task U — LiDAR structural lens (1–2 Aug 2026)

*ID prefix `U-I` checked against this log before use: no collision with the `I-`/`C-`
series. Full detail in `docs/change_reports/TaskU_gate*.md`.*

| Id | Item | Triage |
|---|---|---|
| **U-I11** | **A validity test that silently returned the permissive answer instead of erroring.** `valid_of()` tested `isinstance(nodata, float)`, which is False for `np.float32(nan)`, so the NaN branch never fired, the test fell through to `a != nan` — true everywhere — and the `d5` tile never entered the 5 m mosaic. **Same class as the stored QA verdict (C-08 territory) and `OR IGNORE` (C-09): it looks like it worked.** | **CHANGED NUMBERS.** Fixed 2 Aug; U1/U3/U-Q4a re-run. Found because 2021 height coverage read 51,167 ha — exactly the `d4`-only figure, a number matching another it had no business matching. Blast radius established **by re-running, not reasoning**: the 10 m and 50 cm paths reproduce to the decimal. **AMENDED 4 Aug (LID-1 Y1): THE BLAST-RADIUS SWEEP CHECKED RASTERS AND NOT THE PROSE DERIVED FROM THEM.** *"Only the 5 m height rows moved"* was true — and the R2 density diagnostic **is** a 5 m row. Its 2021 median moved 1.4855 → **1.4672** and its kept-pixel count 20,468,719 → 34,343,805, while **five documents** went on quoting the pre-fix figure for two days. **A re-run is not complete until the sentences that quote its outputs have been re-read.** **Three instances of one shape are now recorded against this entry, and in every one THE ARTEFACT WAS RIGHT AND THE PROSE DESCRIBING IT WAS NOT:** (1) the density median, stale in five documents (Y1); (2) **fourteen `legend_semantics` strings** still asserting *"0 px in 2021"* against the artefact's 1 px, with **U-I15 already recording the 0 → 1 change** — logged and not propagated, corrected under Ruling AG; (3) the summary's §2 product list, which named only `bbn` absent while **`bb8` has no `d5` tile** and six height percentiles were usable rather than seven — correctly recorded at Gate U1 and in the spec's product table, and only the summary implied otherwise (Ruling AH). Corrected in all five at LID-1; no conclusion moves (+38.1% is still ~40%, and U3.6 regresses on block density differences, not this median) |
| **U-I14** | **DOWNGRADED 4 Aug (LID-1, Ruling Z) — ONE observation, not two lines agreeing. STAYS OPEN.** The R6 leg is computed on the **prohibited census temporal p05**: **−17.41 on census p05 against −1.34 on `veg_p05_spatial`** — the same collapse, same direction, as the Bala 29ca claim. What remains is **one structural observation on a 40 ha fragment** (1.9% of the paddock) plus a residual that mostly disappears on the pinned metric. A weaker observation is still an observation, and this row exists so it is picked up on purpose. *Superseded framing, retained:* **Bala 26ca, Riverine — a signal seen twice, independently.** R6 residual **−17.41 pp** (n = 636); U-Q4a makes it structurally poorest of the four, p90 = 0.00, 0th percentile, **at both epochs**. Two products, same paddock, same community, same direction; both small samples, neither built on | **OPEN, named, not investigated.** Deliberately not chased before 10 Aug. Recorded so it is picked up on purpose rather than rediscovered |
| U-I7 | Gate U3 item 1's suggested stable-ground derivation (persistently-zero FPC at both epochs) is **circular for the FPC test** — it forces the difference to zero by construction. Landsat-based derivation substituted | Would have manufactured a false "no sensor offset" pass. Recorded so it is not reintroduced |
| U-I9 | LiDAR `bbh` has **13.33% dynamic range** on-property. Promoted to a finding (S6) as well as a limitation | **To the limitations register.** Any future task scoping LiDAR FPC at Gayini needs this first |
| U-I10 | **There is no stable vegetated ground on Gayini between 2009 and 2021.** A property of the delivery, not a method defect; Gate U2 predicted it | **To the limitations register** as a limitation of the two-epoch LiDAR comparison |
| U-I13 | No stable-ground pixels fall inside the `d4`/`d5` seam, so the seam's vertical behaviour is untested | Matters only if a planar de-trend is ever built (post-10 Aug) |
| U-I1 | `read_registered_layer()` is mandated by CLAUDE.md and referenced in three script headers and four docs but **is defined nowhere in the repo**. Its contract was implemented inline in the Task U scripts | IMPROVE. The convention is currently unenforceable |
| U-I8 | No roads / hardstand / infrastructure layer exists in the repo; stable ground had to be proxied from Landsat persistence | Limits interpretation; changes no number |
| U-I12 | `dim_headline_number` grew 76 → 88 between two Task U registrations. Traced to `rem1_rerender_20260801` (`register_REM1_three_arm_community_pins.py`), another workstream | Benign. Recorded so the jump is not re-investigated |
| U-I15 | R2's 2021 exclusion moved 0 → 1 px after U-I11, so its 3× STOP condition now fires at ratio **218** rather than ∞ | No. Design-seat clearance stands on the substance (0.545 ha vs 0.003 ha). Recorded so the amended number is not read as a reopened decision |

---

## T3 — persistent vegetation surfaces (3 Aug 2026)

*ID prefix `T3-I` checked against this log before use: no collision with the `I-`/`C-`/`U-I` series. Full detail in `docs/change_reports/T3_gate*.md`.*

| Id | Item | Triage |
|---|---|---|
| **T3-I1** | **Reprojecting a thresholded surface is not the same operation as thresholding a reprojected one — log this as a general rule, not a T3 note.** Bilinear-then-threshold and threshold-then-bilinear differ by **16%** on the green-share floor (3,744.20 ha vs the 4,474.03 ha count conversion), and the reprojected figure is **not even stable under scope** (3,363.36 ha over the census footprint). **Any future reprojection of a thresholded surface must declare the order.** The measured area is always the one on the grid where the metric is defined. | **STANDING RULE.** The most reusable thing Gate B2 produced. This is the general form of D8 |
| **T3-I2** | **`veg_p05 IS NULL` matches 0 of 1,080,157 rows.** The census percentile columns encode missing as **float NaN**, not SQL NULL, so an `IS NULL` filter silently admits all 155 NaN pixels and drags MIN/MAX/AVG to NaN. Correct predicate: `(veg_p05 IS NULL OR isnan(veg_p05))`. **Audited 3 Aug: no contamination.** SQLite cannot store NaN (the Python driver converts it to NULL — verified), so every `IS NULL` on a SQLite REAL column is correct by construction; four live fact tables show 0 non-finite among non-NULL. The only parquet/duckdb site, `taskM_gateD_p05_distribution.py:60,71`, already pairs `IS NOT NULL` with `isfinite()`. | **KNOWN-CLEAN, rule recorded.** The trap is confined to the parquet path. The *next* parquet query is the one at risk |
| **T3-I3** | **Two registered files over 50 MB carry whole-file SHA-256 against the first-50-MB convention** — `raster_total_veg_annual_mean_8058` (609.2 MB) and `raster_total_veg_annual_jjason_8058` (579.6 MB), both from T2. A canonical verifier will report drift on unchanged files. The other five >50 MB assets (2 FC stacks, 2 Task U DEMs, the gauge sqlite) are correct. Latent source: `02_build_total_veg_percentile_rasters.R:623-626` uses `digest::digest(file=)`. | **KNOWN-DIRTY, 2 rows.** Fix is a two-row UPDATE; **not applied** — rewriting a registered checksum is a provenance act and wants sign-off |
| T3-I4 | **The GeoPackage silently disagreed with its own raster.** Components are labelled 8-connectivity (queen); polygonising a boolean and casting MULTIPOLYGON→POLYGON splits at every diagonal pinch, i.e. reverts to rook. 40 green-share components became 26 polygons and **219 ha vanished** when sub-parts fell back under the 5 ha filter. Fixed by polygonising a **component-id raster** and dissolving by id; all four layers now reconcile to 0.01 ha. | **CHANGED A NUMBER in a client-facing deliverable.** Adrian overlays the GeoPackage, so this would have been an error he inherited |
| T3-I5 | **No channel or watercourse layer exists anywhere in the project** — not in `spatial_layer_asset` (9 rows), not under `Input/`. The only hydrological geometry is `irrigation_bank_cuts` (1,158 points), which is Task J **irrigation infrastructure, not natural channel**. Gate E's distance-to-channel is therefore NULL and flood frequency is used as a proxy. | **BLOCKS a real test.** The channel-association result is proxy-based until a channel layer is sourced. Substituting the bank cuts would be a category error |
| T3-I6 | FC intermediates (`fc_{total_veg,pv}_3577_wy1988_2023.tif`) carry **no EPSG authority tag**: `crs(x, describe=TRUE)$code` = NA (name `GDA94_Australian_Albers`) though `same.crs(x, "EPSG:3577")` = TRUE. A reader testing the code would conclude the CRS is unknown. | Cosmetic. Changes no number |
| T3-I7 | `taskM_gateD_p05_distribution.py` types `pixel_area_ha=0.0623512`, a 7-sig-fig rounding of `PIXEL_AREA_HA`. Relative error 1.3e-7, immaterial — but it **evades the magic-number lint**, which matches only the exact string `0.062351428`. The lint catches the literal, not the class. | IMPROVE. Suggests the lint should match rounded variants too |

---

## DOC-1 — methods document verification (4 Aug 2026)

*ID prefix `D1-I` checked against this log before use: no collision with the `I-`/`C-`/`U-I`/`T3-I` series. Not to be confused with `DOC1-nnn`, which are claim IDs in `Output/audit/DOC1_claim_check.csv`.*

| Id | Item | Triage |
|---|---|---|
| **D1-I1** | **A concurrent session's commit absorbed this session's staged files — the third occurrence, and the first where the record is now wrong rather than merely untidy.** DOC-1 Gate B staged three explicit paths; before the commit ran, the LID-1 session committed and swept all three into **`b5e3b86`**, whose message describes only LID-1 work. **Nothing was lost and every byte is correct**, but `Output/audit/DOC1_claim_check.csv`, `Output/audit/DOC1_gateB_value_claims.md` and `docs/reports/CC_spec_DOC1_document_verification_v2.md` are now findable only under a LID-1 message. The commit is already pushed, so it was **not** rewritten — CLAUDE.md forbids rebasing pushed work. | **PROCESS, not a number.** The 2 Aug rule ("one worktree, one session, when writes are involved") is correct and was **not followed by either session**. Explicit-path staging does not protect against this — the collision is at commit time, not add time. **The enforceable fix is `git worktree add` for the second session.** Design seat, 4 Aug 2026: **the worktree rule is MANDATORY, not advisory** - a precondition of starting concurrent work. Fourth occurrence; the first to misattribute committed work |
| **D1-I3** | **A code comment that disagrees with its own code, in the script that produces a client-facing figure.** `scripts/12_zone_stratum/T6_gateE_figures.R:112` states *"On Aeolian these are -30.9 and -10.5"*. The adjusted value is right; the raw value is the **pre-correction** figure from before the early-August F6 remediation. The query below it was updated and **the comment was not** — the code computes **−32.044**. | **Changes no number, and would have made a correct document look wrong.** DOC-1 Gate C reproduced −32.044 independently and confirmed the document. Same family as the stored QA verdict (C-08): an assertion that persists while the thing it describes moves. Fix is a one-line comment edit |
| **D1-I4** | **Four figures in the client-facing methods document have no `figure_asset` row — by checksum or by filename.** Figures 2 (`C1_checkerboard_farm.png`), 3 (`S_annual_wet_extent_flow.png`), 5 (`H6_absolute_flood_zones.png`), 8 (`S_floor_and_typical_mapped.png`), all extracted from `Gayini_Veg_samples_ALLPIXEL_v6_20260724.pptx`. **Figure 5 is load-bearing** — the absolute-zones figure that §3.1 and §7.1 both cite as the comparable alternative to the within-community terciles. | **Unregistered artefacts reaching a deliverable.** Found only once the figure manifest made the mapping exact; the other 21 resolve by hash (15) or filename (6). Registration is a design-seat call, not a repository one — the source pptx is the provenance |
| **D1-I5** | **A registered figure that looks like a natural substitute for the percentile fan while implementing a rule the document does not describe.** `FigA_floor_gradient_density.png` draws veg floor against flood frequency with a GAM, is registered and not superseded, and truncates with `ff_cut <- max(supported)` — **the last 5-pp bin anywhere meeting 2,000 cells**, so a sparse bin mid-range is jumped over. The percentile fan uses `cumprod(...) == 1L`, cumulative truncation from the low end. **Two GAM figures, two different truncation rules, two different thresholds, two different bin widths.** | **A registry trap.** Anyone selecting a substitute by reading `figure_asset` without opening the code would swap in the permissive rule. DOC-1 Gate D refused it explicitly. §6.6 names one rule and one threshold; the second figure is undocumented in the methods document |
| **D1-I6** | **Four figures in the methods document are unreproducible, not merely unregistered.** Figures 2 (`C1_checkerboard_farm.png`), 3 (`S_annual_wet_extent_flow.png`), 5 (`H6_absolute_flood_zones.png`), 8 (`S_floor_and_typical_mapped.png`) have **no `figure_asset` row, no file on disk anywhere under `Output/`, and no producing script**. They exist only as bytes inside the docx and the source `Gayini_Veg_samples_ALLPIXEL_v6_20260724.pptx`. Extends D1-I4, which recorded only the missing registry rows. | **Cannot be re-rendered or checked by anyone from this repository.** Figure 5 is load-bearing — the absolute-zones figure §3.1 and §7.1 both cite as the comparable alternative to the terciles. Registration is queued post-10 Aug; the interim fix is the design seat's generated figure-provenance table, which makes the four visibly unregistered rather than silently so |
| **D1-I7** | **A registered figure whose title matches a section it does not serve — the second registry trap, alongside D1-I5.** `T3_A1_two_metrics.png` is titled *"the two floor metrics side by side with their full definitions"* and reads as exactly the figure §4.4 "The two floors" lacks. It is not: its registered caption shows the **green-share floor against the total-cover floor** (the §7.5 / D8 pair), not the **spatial floor against the temporal floor** (the §4.4 pair). **This project has two distinct "two floors" pairs and one figure title that does not say which.** | **Would have put the wrong figure under the section CLAUDE.md calls the single most-confused pair of numbers in the project.** Caught at DOC-2 Gate B by reading the registered caption rather than matching on title. Same family as D1-I5: selecting from `figure_asset` by name without opening the caption or the code. **§4.4 has no registered figure candidate anywhere** |
| D1-I2 | **`Output/tables/taskM_green_at_floor_area.csv` is tidy-long and repeats `threshold`/`mask = green_frac_pct > 50` on every row, including `green_frac_pct_median` and `green_frac_pct_mean`.** Those two rows are computed over all 959,833 valid floor pixels, not over the majority-green subset — a median of that subset would exceed 50 by construction. A reader re-deriving 3.03% from the mask stated on its own row will not reproduce it. | **Changes no number; the value is confirmed.** Source-metadata defect. Same family as the stored QA verdict (C-08): the row asserts a scope it does not have. Fix is to blank the threshold/mask columns on distribution rows |

---

## Notes on discipline

**What I will stop doing** (design seat): raising `IMPROVE` items at STOP points. They arrive looking as urgent as blockers because of where they appear. From here they go straight to this log without interrupting a gate.

**What CC should keep doing:** flagging rather than choosing. Every significant error in this project was caught at a STOP — the wrong gpkg, the twin degeneracy, the Bala confound, the >100 values. The flagging is not the drag; the drag was treating every flag as equally urgent.

**Cadence:** review `BLOCK` and `EXTERNAL` at each task boundary. Everything else weekly.

**ATTRIBUTING AN ERROR CORRECTLY DOES NOT RESOLVE IT** (LID-1, Ruling AB — I-40 family). An error logged against the design seat is still an **open question about the artefact**, not a closed one about the person. **Log the attribution AND the recheck.** Worked case: the 13.33% withdrawal. The figure was checked against `census_by_zone_stratum` — a Landsat census product that could only ever return *absent*, which is not *wrong* (I-42's shape) — and withdrawn, with the source named as the design seat. Naming the source was the correct part and the part that made it worse: attributing it upward **closed** the question instead of opening it. Its denominator, 85,882.6 ha, was in the database the whole time. **The 13.33% is reinstated as a Task U result with its own denominator; I-40's eighth instance stands as logged for the census statement, with this correction recorded against it.**

**ID hygiene:** a proposed issue ID must be checked against the on-disk log before use — never assigned from memory or a prior chat. (I-21/I-22 were mis-referenced from memory twice; the on-disk assignments — I-21 `hardcoded_path`, I-22→C-11 dashboards — are authoritative. The renumbered rows above, C-12 and I-24, took the next free IDs.)
