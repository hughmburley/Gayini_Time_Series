# Addendum AD-A … AD-D — applied where self-contained, held where it depends on P2/P3

**Date:** 3 August 2026 · **Prior:** `6aa345b`
**DB writes:** one in-place caveat amendment (AD-C). No rows added or deleted.

> ## ⚠ THE P2/P3 MESSAGE WAS NEVER RECEIVED
>
> This addendum states *"Everything in the P2/P3 message stands unchanged"*, but **no P2/P3 message
> is in this conversation.** AD-A and AD-D's row assembly are both explicitly *"at P3"*, so they are
> **held** — acting on them would mean inventing P3's scope. The three self-contained items are done.

---

## AD-B — applied, and the ruling recorded

v3 §5's correction block now carries:

> **These counts are a COPY and will go stale the moment anything registers a row — they moved twice
> in one session (82.1% → 82.5%). The Adrian pack workbook REGENERATES them live at build time from
> `dim_headline_number` and `Output/tables/RPTSCOPE_reproduction_status.csv`, and THE WORKBOOK IS
> AUTHORITATIVE OVER THIS REGISTER.**

The correction block itself is left as written — honest for its date. **P4 carries the standing
requirement: the coverage sentence is generated by live query, never typed and never copied.**

## AD-C — applied

`rptscope_canary_p1_paddock_floor_bala29ca`'s `caveat` extended (149 → 553 chars):

> *L-01 APPLIES. This is a WHOLE-PADDOCK value for a paddock spanning three communities whose parts
> behave oppositely (Aeolian level_dev −32.1, Riverine −24.9, Inland −5.8). It is a CANARY FOR QUERY
> DRIFT, NOT a statement about Bala 29ca's condition. Decompose by community before interpreting.*

**Standing-rule tension, flagged not hidden.** Spec §7 says *never modify a registered row*. This
amends a `caveat` on a row registered hours earlier, under an explicit design-seat direction, and it
**only adds a warning** — no value, qualifier or identifier changed, and row counts are identical
before and after (100/297/191/4/59). Precedent: the 31 July precision correction, also a directed
in-place amendment. Recorded so the additive-only rule is not quietly eroded by a second exception.

## AD-D — the limitations register is NOT in the repo. Confirmed.

Your suspicion is right.

| | |
|---|---|
| `Gayini_limitations_register_v10.xlsx` (43 rows) | **ABSENT** — not on disk, not tracked, not in the DB |
| in the repo instead | three **additions** files only: `docs/Gayini_limitations_register_additions_{T2,T6,T12}.md`, all tracked |
| any limitations object in SQLite | **none** |

So the **base register exists only in project knowledge**, and the three committed additions files
are amendments to a document the repo does not contain. Anyone reading the repo alone sees the
amendments and not the thing amended. **That is a live gap for the pack**, since T3 is the sheet
that would draw on it.

**Not logged as a new issue** — it is the same shape as I-17 and the stale-CLAUDE.md case, and
whether it needs a repo copy is a design-seat call, not mine.

## Held, pending the P2/P3 message

**AD-A** — page 3's canary is a cross-check, not a canary, and the choice between (i) registering a
second canary computed by the contract's own SQL and (ii) marking the row as having no canary is a
P3 decision. **Both options are ready to execute**; (i) would trigger the I-39 existence test again.

**AD-D's candidate rows for pack item T3** — the twelve are fully enumerated in your addendum and I
can assemble them with sources on one word, but the instruction places them at P3.

## Probes

| | `dim_headline_number` | `figure_asset` | `raster_asset` | `table_asset` | `report_asset` |
|---|---|---|---|---|---|
| before | 100 | 297 | 191 | 4 | 59 |
| after | 100 | 297 | 191 | 4 | 59 |

Unchanged, as expected for an in-place amendment.
